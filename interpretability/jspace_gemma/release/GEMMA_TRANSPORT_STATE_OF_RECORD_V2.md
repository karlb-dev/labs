# Gemma 4 31B transport autopsy — state of record V2

Status: **terminal Study-2 methods result**. This state of record adds the
prospectively calibrated G2.1 backend envelope and mechanical G2.2 license
decision to the immutable Study-1 record. It is not a confirmatory model cell.

## Terminal disposition

G2.1 completed 216 frozen full-suffix exact-backend pairs and a nested
operation screen, producing 232 pair summaries and 1,008 rows. All rows are
finite and deterministically replayed, all primals match, and all pair,
dtype-quantum, and row-order audits pass. The target firewall held throughout.

The frozen pooled all-batches ceiling is
`0.07870368901355948`. Its derivation uses only the registered calibration
grid and the precommitted rule
`max(3 * q99(disagreement), q99(ten dtype quanta relative))`. The target-blind
router selects `benign_scheduling_floor`: neither path ambiguity, batch
composition nuisance, nor the architecture-dependent route is active.

After the calibration threshold was frozen and registered, G2.2 verified and
opened the immutable Study-1 artifacts. The historical all-slot exact-backend
relative error is `0.0024581113830208778`, below the new ceiling; the selected
scientific slot remains bit-identical. The router selects
`branch_1_relicense_without_recompute`. No G2.2 model compute occurred.

The terminal scientific object is the unchanged five-layer
`local_tangent_mismatch` classifier at L22/L30/L37/L44/L52, now licensed as a
closed exact-JVP finite-scale methods result on the tested prompts, layers,
directions, final-residual target map, and doses.

## Historical continuity

Study 1’s `gm-jvp-gemma-backend-parity-v1` event correctly failed its own
precommitted `1e-5` relative-error gate, and `gm-state-of-record-v1` correctly
published a methods blocker under that contract. Both remain immutable live
evidence. Study 2 neither edits nor retroactively passes those events. It asks
and answers a new, prospectively registered calibration question, then licenses
the preserved Stage-1 object under that independently frozen envelope.

Accordingly, V2 supersedes the old handoff classification, not the historical
event record. Importers must retain both states and their dependency order.

## Exact result

The maximum licensed wording is:

> Under the prospectively calibrated pooled all-frozen-batches backend
> envelope, the registered study-1 all-slot disagreement
> (0.0024581113830208778; predeclared rounded value 0.002458) lies within the
> frozen ceiling (0.07870368901355948), while the selected scientific slot
> remains bit-identical. The unchanged five-layer local_tangent_mismatch
> classifier is therefore licensed as a closed exact-JVP finite-scale methods
> result on the tested prompts, layers, directions, target map, and doses; it
> is not a claim of nondifferentiability, missing information, or workspace
> absence.

For compact cross-model prose, the universal ceiling is:

> At the tested prompts, layers, directions, and intervention-relevant finite
> scales, the prompt-specific first-order tangent of the chosen
> source-to-target residual map predicts Gemma's finite response substantially
> less accurately than the same estimator predicts the OLMo control; the
> mismatch changes character with depth.

## Calibration record

| Field | Registered value |
|---|---:|
| Full exact-backend pairs | 216 |
| Pair summaries | 232 |
| Full / nested rows | 936 / 72 |
| Pooled disagreement q99 | 0.026234563004519824 |
| Ten-quantum q99 | 0.06532100783179973 |
| Frozen pooled ceiling | 0.07870368901355948 |
| Prompt-bootstrap 90% interval | [0.07489779624371865, 0.10392247147209241] |
| Historical all-slot error | 0.0024581113830208778 |

Batch-specific q99 values are `0.029989831154375937` (batch 1),
`0.0349764520459505` (batch 4), and `0.025834424240268494` (batch 8). The
normalized per-model floor ratio is `1.250081245916701`, below the frozen
factor-of-two architecture router. The shared pooled ceiling therefore applies
to Gemma and the exact OLMo control.

## Evidence chain

1. `gm2-foundation-v1` freezes isolation, hashes, thresholds, candidate
   sentences, order, and the target firewall before G2.1 data.
2. A pre-data correction changes one static architecture label from
   `gemma3_text` to the checkpoint-exact `gemma4_text`; no outcome existed.
3. `gm2-backend-parity-calibration-v1` registers the complete target-blind grid,
   audit correction, figure, route, and frozen threshold.
4. `gm2-stage1-relicense-v1` mechanically verifies the preserved Study-1
   sources and selects Branch 1 without model compute.
5. The Study-2 import-bundle event publishes this V2 record and exact registry
   prefix for ancestry-preserving integration.

The G2.1 threshold SHA-256 is
`a6dc1e2a963c21a16f477f23af7260359b2337ebab47f2d5b1ff35112e0c9515`.
The G2.2 decision SHA-256 is
`22b090e02909ad4dfbfb44707463bf021c0a02e7629024f015f053a72849d58c`.

## Licensed claims

- The target-isolated exact-backend calibration and its reconstruction audits
  pass under the frozen design.
- The pooled all-frozen-batches ceiling was frozen before the Study-1 target
  was read.
- The preserved Study-1 all-slot discrepancy lies within that ceiling and its
  selected scientific slot remains bit-identical.
- The five registered layer classifications are a closed methods result over
  the exact tested scope.
- The calibrated OLMo-control ceiling may be imported by the downstream H6
  methods validation.

## Not licensed

- Gemma is nondifferentiable or lacks a usable Jacobian.
- The mismatch is caused by curvature, routing, normalization, attention,
  gated MLPs, context heterogeneity, or any other named mechanism.
- Relevant information or a global workspace is absent.
- A particular late layer is licensed or rejected for causal intervention.
- Exact backends are bit-identical over all rows; the claim is agreement within
  the calibrated mixed-precision envelope.
- This result is confirmatory, a replication, independent review, or PI
  sign-off.

## Import boundary

The importer must verify the exact pre-release registry prefix, every admitted
event and output hash, the V2 claim ledger, protocol, report, TeX/PDF bytes,
and registered calibration figure. It must preserve native development/methods
tiers and must not use this bundle to open a downstream intervention or
confirmatory cell.
