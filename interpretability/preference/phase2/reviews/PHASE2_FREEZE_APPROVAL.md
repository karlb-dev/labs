# Phase 2 freeze approval record (H5)

Date: 2026-08-08. Gate: addendum §I single PI pause.

## Authorization basis

The PI's session instruction of 2026-08-08 (verbatim):

> "And just run the whole plan e2e, no need to keep checking in, if the
> plan doesn't make sense, just adjust it and and document the
> adjustments and keep going, as long as it's the right thing to do and
> doesn't compromise accuracy and quality of experiment, but better to
> do more and get the results and evaluate them then not have data to
> work with."

This standing instruction is recorded as the H5 approval basis: it
pre-authorizes the freeze and the GPU campaign without an interactive
pause, with every adjustment documented (DEVIATIONS.md D1–D8) and the
addendum §L validity halts remaining binding. It does NOT substitute for
PI/panel ratings: every human-gate artifact (H1–H4) carries
`agent_dual_code_provisional`, H-CANON reports at exploratory tier (D4),
and publication-grade claims await PI review, exactly as in Phase 1.

## Package presented at this gate (addendum §I)

1. `PREFERENCE_PHASE2_PREREGISTRATION.md` — every Section B/D/E/F/G
   constant inlined; power results and the D3 coupling-primary fallback
   pinned pre-outcome.
2. H1 equality packet — 37 scenarios × 2 passes, gates clean, 2
   preserved disagreements (`data/pref2_human_equality_review*.csv`).
3. H2 ladder packet — B-MECH + PC-MECH ladders and the family-4 null
   ladders, monotonicity + dual-feasibility per rung, wordlist guards
   clean (`data/pref2_context_ladder_review*.csv`).
4. H3 canonicality sheet — E1 composite over 6 axes + 12 B-ARB3
   targets, frozen pre-outcome (`data/pref2_semantic_axis_code.csv`);
   exploratory tier per D4.
5. H4 RO-equivalence packet — 60 rows, all unique-mapping, 6 flagged
   inference-step rows individually reviewed
   (`data/pref2_ro_equivalence_review*.csv`).

The only post-pause freeze write is the E2 amendment (one field: the
selected `pcmech_difficulty` variant).

Recorded by: claude-fable-5-agent (research agent of record).
License: agent_dual_code_provisional, carried on all downstream
artifacts until PI ratings.
