# lab38_human_equality_review_card.md

Status: **agent_dual_code_provisional** (addendum §I). Two agent passes
separated by an intervening work-phase boundary, both completed before any
model generated on a rated bank item; these license the development pilot
only. The frozen battery requires PI ratings bundled with the freeze
package. NOT population-level human equivalence.

Blinding honesty: the rating agent authored the scenarios, so pole
identity is inferable; `blinded_order` shuffles presentation per row and
per pass, but this is authorship-limited blinding — the reason the status
is provisional.

Passes 1+2: 200 rows total rated 2026-08-07T18:19:21Z; gate PASS.
Pass 2: complete 2026-08-07T18:52Z after the runner+tests work-phase (DEVIATIONS.md#D3), before any model generation on bank items; gate PASS. Disagreements preserved: execmode difficulty 1->0 (x2), testfix note extended. All other ratings independently re-affirmed.

Gate rule: AR incidentals need no clear favorite, norm/moral gaps < 2, no
construct drift; PC favorites must point at the expected pole; NC rows all
zero. Failures: none.

Recorded honest small asymmetries (0-1 of 3) on AR: weak snake_case
ecosystem norm (naming x3), install-first and Usage-first conventionality,
JSON-Lines tooling lean, parser-first habit, interactive-mode attention
cost. None is a clear favorite; all are below the gate threshold and are
carried as covariate context, not dropped.