# PHASE1_FREEZE_APPROVAL.md

**Approved.** On 2026-08-07 (session transcript, after delivery of the
freeze-review packet at commit `11b24d7`), the PI instructed:

> "Yes, keep going with all the planned work and the frozen run; no need
> to stop or check-in until the whole phase 1 work is done."

Recorded dispositions (per the packet's four asks, resolved by the
blanket approval + the packet's non-binding recommendation, which the
approval covers):

1. **Equality ratings** — the provisional agent dual-code passes are
   accepted as the license for the frozen 7B battery. The
   `agent_dual_code_provisional` limitation is carried verbatim in every
   report; a PI/panel pass remains required before any publication-grade
   claim (plan §3.9).
2. **Preregistration constants** — approved unchanged; the candidate is
   frozen as `PREFERENCE_PHASE1_PREREGISTRATION.md`.
3. **Deviations D1–D7** — ratified.
4. **Autonomy** — the agent completes the whole of Phase 1 (frozen
   battery, stop-rule-routed conditional mechanism, DG smoke, 32B
   replication if time permits, closeout) without further check-ins.
   Addendum §M stop-and-ask conditions remain in force for instrument
   alarms (NC alarm, wrong-branch, replay failure, model-pin failure).

Optional blocks dropped under the plan §15.4 drop order, documented here:
proprietary-model DG positive control (no API path in scope), the full DG
battery (smoke only, per plan §12.2), optional lineage (Base/Think
endpoints; §11 is explicitly optional), and temperature robustness. The
32B replication is retained (hardware qualifies: 96 GB).
