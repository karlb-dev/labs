# jspace_lab_olmo_lineage_1_addendum.md — OLMo Lineage Track (branch `interp_jspace_olmo_lineage`)

**Governs with:** `jspace_lab_olmo_lineage_1.md` (operative plan). On conflict, §2–§3 here win. This track runs concurrently with mainline Phase 4 and the Gemma track; the 4_3 §2 parallel contract is binding: branch cut from `3b04173`, package `interpretability/jspace_olmo_lineage/` only, Drive root `.../olmo_lineage_<date>/` only, own append-only registry with `ol-` prefixes, never edit Phase 4/Phase 3 packages, reports, figures, preregistration, or registry. Reusable fixes go in a side compatibility module with a conformance test, upstreamed later.

> **Paste-line for the OLMo agent**
> Read `jspace_lab_olmo_lineage_1.md` in full, then this file, then 4_3 §2 (the parallel contract). Your first responsibility is not your own science: run the two Phase 4 Bank-W capability gates (O1) and ship the early import bundle before anything else, because the mainline freeze waits on them. Then execute O2→O5 in plan order with the amendments below. Everything is development tier; the mainline's governance boundary (no confirmatory outcomes, no self-signing) applies here unchanged.

## 1. Endorsements

The plan is adopted whole, and it is the best-designed side study of the campaign: the six-axis decomposition of "workspace formation" (coordinate availability / sparse capacity / causal utilization / downstream consumption / external-state substitution / temporal organization) replaces the scalar-score trap; the H1–H6 hypothesis set with the outcome router; O1's early-release service obligation; O2's insight that **Base capacity is the first scientific hole** (the trajectory currently shows movement on causal utilization with capacity flat across the post-trained trio — but Base capacity was never measured, so "recruitment of a pre-existing channel" vs. "channel formation" is open exactly there); O3's audit-before-refit rule; O4 as the decisive experiment with §6.6's four competing predicted patterns; and O5's crossed model×lens×readout×activation decomposition.

## 2. Amendments

**2.1 O1 first, unconditionally, and self-contained.** The import bundle for the two capability gates must satisfy the mainline importer's full checklist (source commit reachable, registry hash, per-output rehash, clean worktree, tier preserved, forbidden-uses list). Include the exact Bank-W item manifests and scoring-contract hashes so the mainline joint-support calculation needs nothing else from this branch. Ship it the moment both gates bank — do not batch it with lineage science.

**2.2 Freeze §6.6 as predictions before O4 runs.** The four predicted patterns (external-state substitution / generic difficulty / capacity account / output-adjacent account) are currently narrative. Move them verbatim into the foundation evidence event with per-cell directional signs *before* any O4 intervention cell executes. This is the externalization hypothesis's third timestamp — anticipated (4_1 addendum), observed (trajectory), now **predicted** for an untouched factorial — and it is the difference between a good figure and a claim reviewers cannot dismiss.

**2.3 Checkpoint-inventory task 0 for H5.** H5 (one unobserved stage installs the Think effect) is only testable if intermediate post-training stage checkpoints (SFT / DPO / RLVR stages) are actually published for the OLMo 3/3.1 recipes. Before budgeting any GPU for it: inventory the released artifacts on the hub, register the availability finding as `ol-checkpoint-inventory-v1`, and route — stages available → a minimal two-cell wedge (the stage bracketing the Bank-S onset) enters the queue after O4; unavailable → H5 is demoted to *stated-unresolvable* in the claims table, with the 3.0→3.1 Think delta as the only within-path contrast. Do not let H5's romance eat O4's budget.

**2.4 Expect Base to fail task capability, and say so in advance.** O2's capacity estimand runs on the shared activation corpus and needs no task capability; O4's intervention grid does. Pre-register the expectation that Base drops from many O4 cells via the capability gate, that this is *informative* (the channel's causal-utilization story begins where capability begins), and that Base's O4 row is reported as "gated out" rather than silently absent. This prevents the asymmetric-cohort ghost from Phase 2 reappearing in lineage form.

**2.5 Axis D stays small.** One clean receiver demonstration on the strongest Think Bank-S families — lesion, clean-activation patch rescue, wrong-layer and unrelated-patch controls — beats a receiver grid. Axis D is the pilot for Phase 5A, not a Phase 4 deliverable; cap it at a half-day of GPU and register whatever lands as development evidence for the horizon document.

**2.6 Disk and staging.** Four-plus checkpoints on one VM means rotation: keep at most two resident, verify snapshot manifests on each rehydration, and never stage into the Phase 4 or Gemma roots. The registered own-lenses and base lens for all four checkpoints import by hash — O3's audit rule means zero refits unless an audit finding justifies one, in which case the refit is its own registered decision.

## 3. Success criteria and the Phase 5 hand-off

This track is done for Phase 4 purposes when it has shipped: (1) the O1 bundle (early); (2) the four-checkpoint capacity table with Base filled in and the H1-vs-formation verdict at development tier; (3) the O4 grid with the §6.6 pattern router's output — which of the four accounts survived; (4) the geometry-trajectory figure set with the selection-margin audit; (5) a **lineage claims table** mapping every claim to its tier and to conclusion-skeleton sentences 2 and 4 (training-dependence; externalization), with each sentence's upgrade-or-downgrade resolved or explicitly pending; (6) the final import bundle. The Phase 5 fork this track feeds: if the external-state-substitution account wins O4, sentence 4 graduates and Phase 5B/5C inherit a confirmed mechanism target; if the generic-difficulty or output-adjacent account wins, sentence 4 takes its pre-written downgrade and the Instruct routing question (H3) becomes the 5A entry point instead.

## 4. Risks

(i) The single largest risk is scope: this plan could absorb three VM blocks; the O1→O2→O4 spine is the contract, O3/O5 flex. (ii) Bank-W items are shared with mainline — the import boundary (plan §6.7) must guarantee no untouched Phase 4 family's *intervention* outcome is opened here; capability is the only lawful contact. (iii) The Bank-S trajectory is this track's origin story and its confirmation bias — the §6.6 freeze (2.2) is the antidote; if the pattern dissolves under fact-paired load conditions, that is the finding, and the pre-registered predictions make it publishable rather than embarrassing.
