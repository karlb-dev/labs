# jspace_lab_nextsteps_4_4_addendum.md — Mainline Decision Block (Qwen A1000 resolution → freeze candidate)

**Governs with:** `jspace_lab_nextsteps_4_4.md` (operative plan). On conflict, §2–§3 here win. `jspace_lab_previous_plans.md` is the consolidated historical input; its tail sections remain binding operationally: the durability-deficit register (the two missing historical outputs), the preserved-backup list, the two-pass durability procedure, the Q-L follow-up gating, and the eight review items — which §3 below resolves. If `jspace_lab_previous_plans.md` is not present on this VM, the same binding tail lives in-repo: `interpretability/jspaces/phases/phase4/reports/phase4_part3_summary.md`, `reports/phase4_part3_completion_audit.json`, and `reports/INPROGRESS_VM13_20260802.md` — read those instead; do not proceed without one of the two sources.

> **Paste-line for the mainline agent**
> Read `jspace_lab_nextsteps_4_4.md` in full, then this file, then the tail of `jspace_lab_previous_plans.md` for the operational state. This is a decision block: M0 durability and boundary verification first, then the sealed A1000 postfit queue in its transactional order, then the mechanical Q-L branch, then M2 side-release admission, then M3/M4 only as the branch and reviews permit. Write the Terminal-B pre-commitment note (§2.1 below) before the functional gate opens. No new banks, models, endpoints, or SESOI changes exist in this block, and there is no A2000.

## 1. Endorsements

Adopted whole: the decision-block framing; the narrowed primary table (P4-P1 estimation-only; P4-P2 the sole conditional primary; P4-P3 capability-blocked with the no-dropping-a-passing-model rule); all three terminal states as *successful* outcomes; the five-way Q-L router with the no-branch-shopping rule; M0's durability-before-model-load ordering; the serialized merge lock; the sealed-stage discipline (functional, selection-margin, influence, and canonical stages remain unopened until their transactional turn); M2's importer checklist for the two study-1 bundles; M3's hard review preconditions; and M4's outcome-blind-geometry-before-consumed-shot ordering.

## 2. Amendments

**2.1 Pre-commit the Terminal-B framing before M1 opens.** Write `reports/TERMINAL_B_PRECOMMIT.md` — two paragraphs stating that a no-primary methods freeze is a *success state* whose contributions (operator convergence, measured selection/mechanism sensitivity, the P4-P1 power result, the P4-P3 capability boundary, transport applicability, with Phase 3 remaining the empirical core) are already earned — and commit it before the functional gate runs. Same logic as the Q-L2 amendment draft: when the gate's outcome arrives, the project must be provably indifferent between terminals. The block's one real failure mode is stretching toward Terminal A; this note is the antidote with a timestamp.

**2.2 The P4-P3 capability block is a result; register its sentence.** The 16/20 joint-support shortfall, under the frozen rule that no passing model may be dropped to repair support, is the honest boundary of Bank W as a three-model Phase 4 primary. Register the wording: *"Bank W's load intervention is not lawfully runnable as a cross-model Phase 4 primary at current capability support; it routes to Phase 5B as per-model estimation."* This keeps the load question alive at its earned scope and prevents the freeze candidate from reading as if Bank W silently vanished.

**2.3 M2 before M3/M4, and admit-then-cite.** Admit both study-1 import bundles immediately after the canonical decision banks, with the full importer checklist (reachable source commit, registry hash, per-output rehash, clean-worktree proof, tier preservation, forbidden-uses). The freeze candidate must cite the bundles' claim boundaries verbatim — the Gemma license status and the OLMo trajectory tier — not paraphrase them upward.

**2.4 Approved idle-GPU list only.** The postfit queue is ~70 minutes; this VM will have idle GPU while reviews gate M3/M4. The only approved GPU uses are: the fresh-materialization durability pass 2; the exact A120 capacity-reconstruction recovery (requires ≥75 GB free, is a durability repair, and must not delay the decision boundary); and M5's retained-extremes sensitivity if its producer is already frozen. Idle time is not license to run unfrozen experiments against a canonical lens that is minutes old.

**2.5 Skeleton maintenance is part of the block.** As each stage banks, update `PAPER_CONCLUSION_SKELETON.md` tiers in the same commit: sentence 6 takes its Q-L classification; sentence 3 notes the P4-P1 estimation-only disposition; the P4-P3 boundary sentence from §2.2 enters as a scoped addition. The freeze candidate and the skeleton must never disagree.

## 3. PI resolutions of the eight review items

1. **Q-L branch acceptance:** accepted *conditionally* — the reviewer must read all five registered A1000 stage events before endorsing the mechanical branch; the branch table executes as frozen, and no post-hoc reweighting of sub-gates is permitted.
2. **P4-P2 producer review:** must be genuinely independent — a fresh, narrative-blind session given only the producer code, configs, contracts, and tests (no outcomes, no report sections), returning a written pass/fail against a checklist committed before the review starts. This conversation's design-level endorsement does not substitute for that execution-level review.
3. **Bank-B orthogonal producer review:** same protocol, same blindness, separately registered. The consumed shot runs at most once, after the outcome-blind geometry gate, per M4's priority rule.
4. **Lost `state.json`:** register a permanent-deficit event citing the exhaustive negative search; classify the historical A120–A250 functional event as *partially durable, role superseded* — its gate function was replaced by the A250/A500 and A500/A1000 gates, so no live conclusion depends on it; the durability report stays honestly red on that line forever rather than being edited.
5. **Narrowed family:** confirmed. Terminal A only if every M3 precondition passes without stretching; Terminal B is accepted in advance as a full success (§2.1); no third primary is resurrected.
6. **Addendum-M1 synthesis event:** no separately fabricated event. The existing methods decision record plus sentence-6 wording suffice; if a natural synthesis event falls out of closeout, register it then, unrushed.
7. **A1000 raw-diagnostic archival deviation:** accepted with exact labeling — checkpoint-derived proof is recorded as checkpoint-derived proof, never as recovered terminal text for prompts 1–180.
8. **Signatures:** real independent-review and PI fields only after the complete packet; nothing in this block self-signs.

## 4. Risks

(i) Branch-shopping pressure after seeing the functional gate — the no-shopping rule plus §2.1 are the guard. (ii) The durability pass staying red will feel like a defect; it is the *correct* state while item 4's deficit stands — do not "fix" history. (iii) Two VMs, one Drive account: the sidelines VM has its own roots; if quota pressure appears, the mainline's registered artifacts win and the sidelines pause staging. (iv) If Q-L4/Q-L5 lands, resist same-day replanning: Terminal C's write-up is already specified, and the correct next move is the freeze candidate, not a new instrument.
