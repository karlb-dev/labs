# jspace_lab_nextsteps_4_5.md

## J-space Phase 4, closeout block 5: admit the terminal side-study records, complete release governance, and freeze Terminal C

**Status:** operative closeout plan for the merged `interp_jspace_part2` campaign. This is a CPU-first release and governance block. It is not a new discovery block, not a Phase 5 experiment, and not authorization to open a confirmatory or replication intervention outcome.

**Source branch:** `interp_jspace_part2`, at the exact remote HEAD observed at launch.

**Working branch:** `interp_jspace_phase4_5`, created from that exact source HEAD.

**Primary namespace:** `interpretability/jspace_phase4/`

**Expected compute:** CPU, storage, hashing, TeX, and repository work only. Do not reserve an expensive GPU runtime for this plan. No model-scale producer is authorized.

**Target terminal tag:** `jspace-phase4-frozen-v1`, created only after the real independent-review, permanent-deficit, and PI approval gates below have passed.

**Governs with, in this order:**

1. `jspace_lab_nextsteps_4_4.md`
2. `jspace_lab_nextsteps_4_4_addendum.md`
3. `interpretability/jspace_phase4/reports/INPROGRESS_VM14_20260803.md`
4. `interpretability/jspace_phase4/reports/PHASE4_PART4_EXECUTION_RECORD.md`
5. `interpretability/jspace_phase4/reviews/READY_FOR_PHASE4_FREEZE_REVIEW.md`
6. `interpretability/jspace_phase4/preregistration/FREEZE_GATE_LEDGER_PHASE4.md`
7. `interpretability/jspace_phase4/paper/PHASE4_METHODS_DECISION_RECORD.md`
8. `interpretability/jspace_phase4/paper/PAPER_CONCLUSION_SKELETON.md`
9. `interpretability/reviews/JSPACE_SIDELINES_2_INTEGRATION_RECORD.md`
10. `interpretability/jspace_gemma/release/GEMMA_TRANSPORT_STATE_OF_RECORD_V2.md`
11. `interpretability/jspace_gemma/release/IMPORT_BUNDLE_SIDELINES2.{json,md}`
12. `interpretability/jspace_olmo_lineage/reports/OLMO_LINEAGE_STATE_OF_RECORD_V2.md`
13. `interpretability/jspace_olmo_lineage/release/IMPORT_BUNDLE_SIDELINES2.{json,md}`

On conflict about a registered result, the append-only source registry and exact release bundle win. On conflict about Phase 4 governance, the existing frozen routers and the real external-review fields win. This plan may close stale narrative debt and admit methods-only bundles, but it may not reopen Q-L4, restore a retired primary, change a threshold, or convert a capability-gated absence into a zero.

> ## Paste-line for the coding and research agent
>
> Read every governing file above before changing code. Fetch and fast-forward `interp_jspace_part2`, require a clean tree, record the exact source SHA, and create `interp_jspace_phase4_5` from that SHA. Phase 4.4 is scientifically complete: the canonical decision is Q-L4, Terminal C; no single sparse Qwen lens is nominated; P4-P1 is estimation-only; P4-P2 is removed; P4-P3 is blocked; zero confirmatory tests entered the Holm family. The two Study-2 side branches are already ancestry-merged and must not be merged again. Your first job is to verify and admit their exact terminal bundles into the Phase 4 closeout record at their native methods/development tiers, without copying native `gm2-*` or `ol2-*` events into the Phase 4 registry. Your second job is to obtain a genuinely fresh Drive materialization, reproduce the known-deficit-aware durability state, and prove that there are zero unexpected deficits. Your third job is to assemble a narrative-blind independent-review packet and leave real reviewer and PI fields for real humans or separately authorized sessions. Only after those fields pass may you create the dedicated freeze record, merge the closeout branch into `interp_jspace_part2`, rerun all suites, create the annotated tag `jspace-phase4-frozen-v1`, and push branch plus tag. Do not load a 27B or 32B model, run A2000, author a retained-extremes producer, repair Bank W by subset selection, refit a lens, open M3/M4, or begin Phase 5. Stop immediately on any unexpected hash, ancestry, namespace, untouched-data, or registry discrepancy.

---

# 0. Executive decision

## 0.1 Phase 4 has reached its scientific terminal state

The Phase 4.4 queue answered the only remaining mainline decision question.

| Object | Terminal result | Consequence |
|---|---|---|
| A500 to A1000 structural comparison | PASS, task q50/q05 `0.998702 / 0.998122` | The averaged operator is strongly converged on the registered assay. |
| Selected token identity | FAIL, Jaccard `0.538462` | Literal sparse rows are not invariant. |
| Selected subspace | FAIL, normalized projector overlap `0.709818` | Instability is not merely token aliasing. |
| Bridge-rescue endpoint | FAIL, difference `-0.294028` nat | A load-bearing causal conclusion remains fit-sensitive. |
| Selection-margin audit | COMPLETE, all `17,381` positions retained | Near ties do not rescue the sparse instrument. |
| Prompt-323 influence | NEGLIGIBLE under every frozen materiality gate | The retained heavy row does not explain Q-L4. |
| Mechanical canonical router | **Q-L4** | No single sparse Qwen lens is nominated. |
| P4-P1 | estimation-only | No Phase 4 binary bridge primary. |
| P4-P2 | removed by Q-L4 | No consumed pilot or untouched intervention opened. |
| P4-P3 | blocked at `16/20` | No cross-model Bank-W intervention. |
| Holm family | zero opened tests | No alpha transfer or post-hoc replacement. |

The correct terminal label is therefore:

> **Terminal C: a development/methods instrument study with no Phase 4 confirmatory primary.**

Phase 4.5 does not ask whether this outcome can be made prettier. It asks whether the record can be frozen, reproduced, cited, and handed to paper analysis without ambiguity.

## 0.2 The Study-2 sidelines sharpen the methods boundary but do not reopen a primary

### Gemma Study 2

The target-blind G2.1 calibration completed 216 full-suffix backend pairs plus the nested operation screen. The frozen pooled ceiling is:

```text
0.07870368901355948
```

The preserved Study-1 all-slot disagreement is:

```text
0.0024581113830208778
```

It lies within the new prospectively calibrated envelope, and the selected scientific slot remains bit-identical. The mechanical route is `branch_1_relicense_without_recompute`. The unchanged five-layer `local_tangent_mismatch` classifier is now a closed exact-JVP finite-scale methods result over its tested scope.

This does **not** license nondifferentiability, a named mechanism, a missing-information claim, workspace absence, or a Gemma causal intervention.

### OLMo Study 2

The official Think-SFT and Think-DPO checkpoints pass technical conformance but fail the prospective Bank-S capability population:

| Checkpoint | Overall capability | Bank-S capability | Direct plus composed Bank-S facts | Intervention opened |
|---|---:|---:|---:|---|
| Think-SFT | `0.00617` | `0.00833` | `0` | no |
| Think-DPO | `0.00309` | `0.00278` | `0` | no |

The stage effects are therefore missing, not zero. The router returns `null_or_unresolved`; no SFT or DPO localization is licensed and no Tier-2 lens refit is allowed.

H6 finite-dose transport also closes negatively at the in-band layers. Neither Base nor OLMo-3.1 Think reaches the frozen 0.90 passage floor at L24/L32/L40 on the tested epsilon ladder. OLMo-3.1 Think passes only the L56 late anchor at epsilon 0.10. This bounds transport wording but does not retroactively invalidate the paired projection-ablation findings.

The OLMo Think/Instruct Bank-W pair planning audit has `0.7788` power at the existing 16 common capable families, below the frozen 0.80 target; the first simulated passing count is 18. No intervention is authorized from the current design.

## 0.3 Why Phase 5 does not open in this block

A legitimate Phase 5 needs one prospectively specified mechanism question whose expected information gain justifies new GPU work. The current candidate branches all need design work first:

1. **Qwen consensus/cross-fit successor:** Q-L4 says the current sparse instrument is decision-sensitive. A new consensus, ensemble, or lens-random-effect estimand must be designed and reviewed before any model load.
2. **OLMo Bank W:** the pair design is close to powered but not powered. New capable families require outcome-blind authoring, a new capability cycle, a new partition, and a new preregistration.
3. **OLMo stage attribution:** the official intermediates cannot perform the frozen assay. A capability-compatible stage assay would be a new measurement protocol, not a retry.
4. **Gemma mechanism localization:** Stage 1 is now licensed as a methods result, but attention, normalization, MLP, routing, and context-heterogeneity accounts remain unseparated. A broad G2-G5 safari would be hypothesis shopping.
5. **Actual intervention-dose transport:** the required per-site dose archive was never recorded. Recollecting it is a new prospective experiment.

The next rational step is to freeze the campaign boundary, analyze the complete evidence graph offline, and reopen GPU work only if that analysis selects one branch under a written router.

## 0.4 Successful terminal outputs

This block succeeds when it produces all of the following:

- exact methods-only admission of the Gemma and OLMo Study-2 bundles;
- Phase 4 narrative documents updated to the actual terminal side-study state;
- a fresh-materialization durability proof with zero unexpected deficits;
- explicit external acceptance or rejection of the permanent historical deficit;
- a narrative-blind independent protocol and untouched-data review;
- explicit PI acceptance of Terminal C and the no-primary disposition;
- a frozen state-of-record, release manifest, clean merge, and annotated tag;
- a handoff to `paper_analysis.md`, not to a Phase 5 GPU runner.

---

# 1. State of record at launch

The values below are expected. The agent must regenerate and verify them from the live branch, registries, bundles, and hashes at launch.

## 1.1 Repository and ancestry

Expected integrated history:

- Phase 4.4 closeout branch was merged with ancestry preserved.
- Gemma Study 2 and OLMo Study 2 are direct ancestors of `interp_jspace_part2`.
- Gemma precedes OLMo in the side dependency order.
- No branch was squashed.
- No native `gm2-*` or `ol2-*` event entered the Phase 4 registry.
- Integration-only verifier fixes changed no scientific artifact or registry row.

Required launch artifact:

```text
interpretability/jspace_phase4/reports/PHASE4_PART5_FOUNDATION.json
```

It must record:

- exact source HEAD;
- clean-tree proof;
- branch parent;
- all relevant merge commits and second parents;
- source registry hashes and line counts;
- exact side-bundle hashes;
- exact source state-of-record hashes;
- test counts at launch;
- Drive mount identity and materialization provenance;
- a declaration that no model-scale process is active.

## 1.2 Mainline Phase 4

Expected live evidence includes:

```text
p4-qwen-lens-fit-drawA-n1000-dev-v1
p4-qwen-lens-convergence-drawA-n500-n1000-dev-v1
p4-qwen-multilens-functional-gate-a500-a1000-published-dev-v1
p4-qwen-selection-margin-a500-a1000-dev-v1
p4-qwen-lens-influence-prompt323-dev-v1
p4-qwen-canonical-lens-decision-a1000-dev-v1
p4-import-olmo-bank-w-capability-v1
p4-bank-w-capability-joint-imported-dev-v1
p4-import-gemma-transport-v1
p4-import-olmo-lineage-final-v1
p4-qwen-a120-a250-state-permanent-deficit-v1
```

Expected terminal branch:

```text
Q-L4
```

Expected current mainline imports refer to the Study-1 side releases. Study-2 histories are repository-integrated but not yet admitted into the Phase 4 closeout registry.

## 1.3 Gemma Study 2

Expected terminal objects:

```text
interpretability/jspace_gemma/release/GEMMA_TRANSPORT_STATE_OF_RECORD_V2.md
interpretability/jspace_gemma/release/gemma_transport_claim_ledger_v2.md
interpretability/jspace_gemma/release/TRANSPORT_GATE_PROTOCOL_V2.md
interpretability/jspace_gemma/release/IMPORT_BUNDLE_SIDELINES2.json
interpretability/jspace_gemma/release/IMPORT_BUNDLE_SIDELINES2.md
interpretability/jspace_gemma/release/evidence_events_prefix_sidelines2.jsonl
```

Expected terminal event:

```text
gm2-sidelines2-import-bundle-v1
```

Expected bundle SHA-256:

```text
9ef48b8ab1d99d52a756ddea1e285a9d61e781fd054cbf92702bfe81be56f5b0
```

Expected frozen source-prefix SHA-256:

```text
2a144bcf0e7be0ac4307f7e2a2984c1879340b9a7e9278d10143d122a14fd30a
```

## 1.4 OLMo Study 2

Expected terminal objects:

```text
interpretability/jspace_olmo_lineage/reports/OLMO_LINEAGE_STATE_OF_RECORD_V2.md
interpretability/jspace_olmo_lineage/reports/OLMO_LINEAGE_CLAIMS_TABLE_V2.md
interpretability/jspace_olmo_lineage/release/IMPORT_BUNDLE_SIDELINES2.json
interpretability/jspace_olmo_lineage/release/IMPORT_BUNDLE_SIDELINES2.md
interpretability/jspace_olmo_lineage/release/evidence_events_prefix_sidelines2.jsonl
```

Expected terminal event:

```text
ol2-sidelines2-import-bundle-v1
```

Expected bundle SHA-256:

```text
c213dc74aa78dcd6613c8bd1562dd07d2e2a0345409ee6da585001693d8e6b1c
```

Expected frozen source-prefix SHA-256:

```text
0a8973e01d562a82fa88da650ab8597c140050f6caf46c8bbd72e2b58acffb58
```

## 1.5 Durability and external gates

Expected state before Phase 4.5:

- exact A120 capacity tensor recovered at its registered hash;
- one historical A120-A250 operational `state.json` permanently missing;
- two same-mounted audits agree on one known deficit and zero unexpected deficits;
- no independent fresh materialization has yet been proven;
- independent review is unsigned;
- permanent-deficit acceptance is unsigned;
- PI disposition is unsigned;
- no freeze tag exists.

Any deviation from this expected state must be recorded before continuing.

---

# 2. Branch, namespace, and write contract

## 2.1 Create one closeout branch

```bash
set -euo pipefail

cd /content/labs
git fetch origin
git switch interp_jspace_part2
git pull --ff-only origin interp_jspace_part2
test -z "$(git status --short)"

PARENT_SHA=$(git rev-parse HEAD)
git switch -c interp_jspace_phase4_5
printf '%s\n' "$PARENT_SHA" > /content/phase4_5_parent_sha.txt
```

Do not create new scientific side branches. Both Study-2 histories are already merged.

## 2.2 Allowed repository writes

Primary allowed writes:

```text
interpretability/jspace_phase4/
  configs/                         # import/closeout configs only
  jspace_phase4/                   # import, inventory, release helpers only
  manifests/
  paper/
  preregistration/
  protocol/
  release/
  reports/
  reviews/
  tests/

interpretability/reviews/          # one campaign integration/freeze record if needed
```

Permitted updates in side namespaces are limited to an independently found documentation or verifier defect that is necessary to reproduce an already registered bundle. Such a correction must not change any source registry row, threshold, result payload, state-of-record claim, or release hash. Prefer no side-tree edits.

## 2.3 Forbidden writes and actions

- no Phase 2 or Phase 3 registry edits;
- no native `gm2-*` or `ol2-*` event in the Phase 4 registry;
- no rewrite of the existing Study-1 Phase 4 imports;
- no mutation of registered side events or bundle bytes;
- no model download, model load, lens fit, JVP run, intervention, generation, or ablation;
- no A2000;
- no new bank or family authored inside this closeout branch;
- no SESOI, threshold, endpoint, population, or model-set change;
- no conversion of missing stage effects to zero;
- no use of the SFT/DPO incapable rows as an intervention cohort;
- no Bank-W subset selection to manufacture 18 families;
- no self-signed reviewer or PI field;
- no freeze tag before the external gates pass.

## 2.4 Append-only closeout evidence

Every new Phase 4 event must:

- have a new `p4-*` evidence ID;
- preserve the source native tier;
- cite exact bundle and source-prefix hashes;
- list allowed and forbidden Phase 4 uses;
- contain no model-derived recomputation;
- write to a new output path;
- pass schema, ancestry, path, and namespace checks;
- be locally backed up and pushed before the next stage.

---

# 3. F0: foundation and terminal-state verification

**Compute:** CPU only.

**Purpose:** prove that the branch starts from the exact integrated state and that the closeout agent is not reconstructing history from prose.

## 3.1 Verify branch ancestry

Programmatically verify:

1. the Phase 4.4 merge is reachable;
2. Gemma Study 2 terminal commit is reachable;
3. OLMo Study 2 terminal commit is reachable;
4. the merge commits retain the expected second parents;
5. the current source is not behind remote;
6. no unexpected merge or registry rewrite occurred after the recorded integration boundary.

## 3.2 Verify all three registries independently

Run each package verifier under the merged worktree:

```bash
bash interpretability/jspace_gemma/repro.sh
python -m jspace_gemma verify

bash interpretability/jspace_olmo_lineage/repro.sh
python -m jspace_olmo_lineage verify

cd interpretability/jspace_phase4
python -m pytest -q
python -m jspace_phase4 verify
```

Use the exact repository commands when package entry points differ. Record command, environment, result, and registry hash.

## 3.3 Rehash the Study-2 bundles

For each bundle:

- verify source branch and commit;
- verify source commit reachability;
- verify frozen registry-prefix bytes and hash;
- verify every source event named by the bundle;
- verify every admitted output size and SHA-256;
- verify source tree cleanliness at production;
- verify native tier;
- verify forbidden uses;
- verify deterministic PDF/TeX/figure hashes where registered;
- verify the current live append-only registry has not mutated the frozen prefix.

## 3.4 Register foundation

Proposed event:

```text
p4-phase4-part5-foundation-v1
```

Outputs:

```text
reports/PHASE4_PART5_FOUNDATION.json
reports/PHASE4_PART5_FOUNDATION.md
manifests/phase4_part5_source_inventory.json
```

**F0 gate:** any unexpected ancestry, namespace, hash, output, event-prefix, or tier discrepancy stops the plan.

---

# 4. F1: admit the two Study-2 terminal bundles

**Compute:** CPU only.

**Purpose:** bring the final side-study claim boundaries into the Phase 4 closeout record without promoting them and without rewriting the Study-1 imports.

## 4.1 Why a second import layer is needed

Repository ancestry integration and scientific admission are different operations.

The existing Phase 4 registry contains Study-1 imports:

```text
p4-import-gemma-transport-v1
p4-import-olmo-lineage-final-v1
```

The merged branch also contains later Study-2 terminal bundles. The Phase 4 paper skeleton and methods record still describe the earlier terminal states. Freezing without a methods-only Study-2 admission would leave the freeze packet internally stale.

The Study-2 imports may update only the terminal methods/development boundary. They cannot alter Q-L4, restore a primary, open an intervention, or raise evidence tiers.

## 4.2 Gemma Study-2 import

Proposed event:

```text
p4-import-gemma-transport-study2-v1
```

Required dependency:

```text
p4-import-gemma-transport-v1
```

Required imported meaning:

> The prior `1e-5` parity blocker remains historically correct under its own contract. A later target-blind calibration froze an all-batches mixed-precision ceiling of `0.07870368901355948`; the preserved all-slot error `0.0024581113830208778` lies below it and the selected slot is bit-identical. The unchanged five-layer `local_tangent_mismatch` classifier is therefore a closed exact-JVP finite-scale methods result over the tested scope. No named mechanism, nondifferentiability, missing-information, workspace, or intervention conclusion is licensed.

Required forbidden uses:

- not confirmatory;
- not replication;
- not a Gemma workspace result;
- not a late-layer intervention license;
- not proof of curvature as the unique cause;
- not permission to reopen G2-G5 inside Phase 4.

## 4.3 OLMo Study-2 import

Proposed event:

```text
p4-import-olmo-lineage-study2-v1
```

Required dependency:

```text
p4-import-olmo-lineage-final-v1
p4-import-gemma-transport-study2-v1
```

Required imported meaning:

1. The official Think-SFT/DPO wedge is technically valid but capability-gated, with zero Bank-S facts capable on both direct and composed variants at either checkpoint. Stage effects are missing, not zero; SFT versus DPO is unresolved.
2. H6 does not license an in-band finite-dose regime at L24/L32/L40 on Base or OLMo-3.1 Think over the frozen ladder. Think passes only the L56, epsilon 0.10 late anchor.
3. The exact site-dose coverage required to map registered causal interventions onto the H6 ladder is unavailable in the historical archive.
4. The OLMo Think/Instruct Bank-W pair design has power `0.7788` at 16 common capable families; 18 is the first simulated passing count. No intervention is authorized from the current design.
5. Study-1 sentence 2 remains an architecture-matched association; sentence 4 remains pending.

Required forbidden uses:

- not stage attribution;
- not evidence for no SFT or DPO effect;
- not an externalization result;
- not a Bank-W null;
- not permission to reauthor or subset families inside Phase 4;
- not invalidation of the registered paired ablation effects;
- not a transport license for O5 or crossed-lens patching.

## 4.4 Import implementation

Add a dedicated closeout importer rather than overloading the Study-1 import event:

```text
jspace_phase4/experiments/p4_import_sidelines_study2.py
configs/p4_import_gemma_transport_study2.yaml
configs/p4_import_olmo_lineage_study2.yaml
```

The importer must:

- bind exact source bundle hashes;
- bind exact frozen prefix hashes;
- bind the source terminal state and claims table;
- rehash every selected output in the merged worktree;
- reject a dirty tree;
- reject native event leakage;
- reject tier promotion;
- reject a missing dependency import;
- reject any `phase4_use` beyond methods/narrative boundary;
- write one Phase 4 import event per bundle;
- preserve an exact local backup of every registered output.

## 4.5 Update closeout narratives in the same evidence boundary

After each import event is registered, update these files in the same commit or an immediately following narrative-only commit:

```text
paper/PAPER_CONCLUSION_SKELETON.md
paper/PHASE4_METHODS_DECISION_RECORD.md
reports/PHASE4_DEVELOPMENT_REPORT.md
preregistration/FREEZE_GATE_LEDGER_PHASE4.md
preregistration/SCIENTIFIC_PREREGISTRATION_PHASE4_CANDIDATE.md
manifests/parallel_import_inventory.md
reviews/READY_FOR_PHASE4_FREEZE_REVIEW.md
```

Required narrative changes:

- sentence 5 changes from an unresolved backend blocker to a calibrated, scoped finite-scale mismatch result;
- sentence 2 states that the official wedge did not localize the transition because prospective capability cohorts were empty;
- sentence 4 states that Bank W remains unresolved and the OLMo-pair redesign is not powered at current support;
- H6's in-band failure and late-anchor-only result are recorded at methods tier;
- Q-L4 and Terminal C remain unchanged;
- the mainline freeze still contains zero confirmatory primary tests.

Do not rewrite the historical Study-1 descriptions out of the record. Show the dependency chain from blocker to later calibration/relicense.

## 4.6 F1 acceptance tests

At minimum, test:

- exact bundle happy path;
- mutated source prefix rejection;
- later append-only source registry acceptance only when frozen prefix is exact;
- missing Study-1 dependency rejection;
- tier promotion rejection;
- forbidden `phase4_use` rejection;
- native `gm2-*`/`ol2-*` leakage rejection;
- duplicate import rejection;
- deterministic output ordering;
- live merged-worktree materialization;
- narrative files cite the new event IDs and do not cite native side IDs as Phase 4 evidence.

**F1 gate:** both import events are registered, pushed, backed up, and independently reconstructable before durability begins.

---

# 5. F2: fresh-materialization durability proof

**Compute:** CPU, storage, and hashing.

**Purpose:** close the only mechanical release gate that the Phase 4.4 VM could not close.

## 5.1 A genuinely fresh boundary is mandatory

Use a new VM or a new environment whose Drive files are freshly materialized. Merely rerunning on the same mounted cache is not independent proof.

Preferred sequence:

1. push F0/F1 to remote;
2. terminate the original runtime cleanly;
3. launch a fresh CPU-capable Colab or equivalent VM;
4. freshly mount Drive;
5. clone or fetch the repository from remote;
6. verify local/remote equality;
7. run the durability pass without copying the previous pass JSON into the materialization root.

A GPU may be physically attached by the platform, but no CUDA/model work is authorized.

## 5.2 Expected durability result

The historical `state.json` deficit cannot become present through wishful hashing. A successful fresh pass is therefore:

```text
all expected current outputs verified
one exact previously declared permanent deficit
zero unexpected deficits
zero wrong hashes
zero pin conflicts
zero path-policy violations
zero temporary/recovery outputs registered
```

Do not require the impossible statement `419/419` when one object has an approved permanent-deficit path. Instead distinguish:

```text
raw_all_live_outputs_verified = false
policy_aware_release_set_verified = true
```

The policy-aware result becomes true only when:

- the fresh materialization reproduces the same one known deficit;
- every other reference verifies;
- the append-only deficit event is exact;
- an independent reviewer accepts the permanent-deficit disposition;
- the PI accepts the release with that explicit limitation.

## 5.3 Required commands

Use the repository's current durability CLI. The intended shape is:

```bash
export JSPACE4_RUN_ROOT=/content/drive/MyDrive/interpret/special-lab-1/phase4_20260731

python -m jspace_phase4.durability \
  --known-deficits interpretability/jspace_phase4/protocol/KNOWN_DURABILITY_DEFICITS_PHASE4.json \
  --pass-label phase4-part5-fresh-materialization \
  --output /content/PHASE4_PART5_DURABILITY_FRESH.json

python -m jspace_phase4.pre_freeze_inventory \
  --pass-label phase4-part5-fresh-materialization
```

Adjust only to the actual CLI. Do not relax policy or omit large files because hashing is slow.

## 5.4 Required outputs

```text
reports/PHASE4_PART5_DURABILITY_FRESH.json
reports/PHASE4_PART5_DURABILITY_FRESH.md
manifests/phase4_pre_freeze_inventory_v4_5.json
manifests/phase4_pre_freeze_inventory_v4_5.md
reviews/PHASE4_PERMANENT_DEFICIT_REVIEW_PACKET.md
```

The deficit packet must include:

- original evidence event and output role;
- exhaustive negative-search record;
- why timing fields make synthesis invalid;
- the later events that supersede its decision role;
- all exact hashes of surviving outputs;
- fresh-materialization proof;
- explicit risk statement;
- reviewer and PI decision fields.

## 5.5 F2 stop rules

Stop immediately if:

- any unexpected output is absent;
- any present output has the wrong hash;
- the known deficit set grows;
- a source registry hash differs unexpectedly;
- a model/lens artifact resolves to a different snapshot;
- a registered path points to temporary or recovery-only storage;
- a Study-2 import output fails fresh materialization;
- the new inventory contains a native side ID in the Phase 4 registry.

**F2 gate:** the exact known-deficit pattern is reproduced from a fresh materialization with zero unexpected discrepancies.

---

# 6. F3: final freeze packet and independent review

**Compute:** CPU only.

**Purpose:** turn the mechanical closeout into a reviewable scientific release without asking the implementation agent to grade itself.

## 6.1 Freeze packet contents

Create or refresh:

```text
reviews/PHASE4_FREEZE_REVIEW_INDEX.md
reviews/QWEN_A1000_CANONICAL_REVIEW_PACKET.md
reviews/P4_P2_PRODUCER_REVIEW_PACKET.md
reviews/BANK_B_ORTHOGONAL_REVIEW_PACKET.md
reviews/PHASE4_PERMANENT_DEFICIT_REVIEW_PACKET.md
reviews/PHASE4_SIDELINES_STUDY2_ADMISSION_REVIEW.md
reviews/PHASE4_UNTOUCHED_DATA_AUDIT.md
reviews/PHASE4_INDEPENDENT_REVIEW_TEMPLATE.md
reviews/PHASE4_PI_DISPOSITION_TEMPLATE.md
```

The P4-P2 and Bank-B packets remain historical non-applicable records. They must not be reframed as partially executed work.

## 6.2 Narrative-blind independent review protocol

Use a fresh agent/session or human reviewer who is given only:

- frozen producer and analysis code;
- configs and thresholds;
- exact registered result tables;
- event registry and supersession graph;
- tests;
- bundle manifests;
- durability/inventory outputs;
- the claim-boundary checklist.

Do not initially provide the aspirational paper narrative or desired conclusion.

The reviewer must independently verify:

1. Q-L4 is the unique output of the frozen branch table.
2. Structural pass and sparse/causal failures use the registered metrics and thresholds.
3. Selection-margin strata do not rescue the canonical instrument.
4. Prompt-323 influence is negligible under the frozen materiality rules and is correctly scoped to the current runtime.
5. P4-P1, P4-P2, and P4-P3 dispositions follow mechanically.
6. No confirmatory or replication intervention outcome exists.
7. The Study-2 imports preserve source tiers and forbidden meanings.
8. Gemma V2 relicensing does not erase the historical blocker event.
9. OLMo wedge effects are missing, not zero.
10. H6 failure does not get restated as invalidation of paired ablation evidence.
11. The Bank-W pair result is a power/design closure, not an externalization null.
12. The fresh durability proof contains only the known permanent deficit.
13. All figures and tables in the freeze packet regenerate from registered data.
14. The proposed terminal prose does not exceed any claim ledger.

## 6.3 Independent-review output

Required file:

```text
reviews/PHASE4_INDEPENDENT_REVIEW_<date>.md
```

Required fields:

```text
reviewer identity or session identifier
review date and source commit
materials supplied
narrative-blind declaration
checks performed
reconstructed numbers
PASS / PASS WITH EXPLICIT LIMITATION / FAIL per gate
permanent-deficit decision recommendation
Terminal C recommendation
signature or cryptographic/session provenance
```

A review that merely says “looks good” does not pass.

## 6.4 Untouched-data audit

The independent review must search:

- every relevant registry;
- Drive run roots;
- repository result mirrors;
- logs and recovery directories;
- branch history;
- side-study roots.

It must verify that no Phase 4 confirmatory or replication intervention result exists and that no agent opened a sealed family while preparing Phase 4.4 or Phase 4.5.

Required output:

```text
reviews/PHASE4_UNTOUCHED_DATA_AUDIT.md
```

**F3 gate:** all independent-review checks pass or pass with only the already declared permanent/runtime limitations.

---

# 7. F4: PI disposition

**Compute:** none.

**Purpose:** obtain an explicit scientific decision from the principal investigator rather than treating silence as consent.

## 7.1 Required PI decisions

The PI must explicitly accept or reject each item:

1. **Terminal C:** Phase 4 freezes as a no-primary development/methods study.
2. **Q-L4:** no single sparse Qwen A1000 lens is canonical.
3. **No A2000:** no automatic fit-size continuation exists.
4. **P4-P1:** estimation-only.
5. **P4-P2:** removed by Q-L4.
6. **P4-P3:** blocked at 16/20.
7. **Zero Holm tests:** no confirmatory alpha family was opened.
8. **Permanent deficit:** accept or reject release with the missing historical `state.json` explicitly recorded.
9. **Prompt runtime limitation:** accept current-runtime influence result without historical-runtime identity.
10. **Gemma Study 2:** accept the calibrated finite-scale methods result and its forbidden meanings.
11. **OLMo Study 2:** accept capability-gated wedge, in-band H6 failure, and underpowered pair boundary.
12. **Phase 5 pause:** freeze and move to offline paper analysis before authorizing more GPU work.
13. **Tag authorization:** authorize or reject `jspace-phase4-frozen-v1`.

## 7.2 Required PI output

```text
reviews/PHASE4_PI_DISPOSITION_<date>.md
```

It must identify:

- exact commit reviewed;
- independent-review file and hash;
- each accepted/rejected item;
- any required correction;
- explicit freeze authorization;
- PI identity and date.

Do not generate a signature on the PI's behalf. The agent may prepare the template and verify its completeness after it is genuinely filled.

**F4 gate:** explicit authorization exists at the exact reviewed commit.

---

# 8. F5: freeze record, release manifest, merge, and tag

Runs only after F4 passes.

## 8.1 Create the terminal state of record

Required files:

```text
reports/PHASE4_STATE_OF_RECORD.md
release/PHASE4_RELEASE_MANIFEST.json
release/PHASE4_RELEASE_MANIFEST.md
release/PHASE4_CLAIM_LEDGER.md
release/PHASE4_REPRODUCTION_GUIDE.md
release/PHASE4_KNOWN_LIMITATIONS.md
```

### `PHASE4_STATE_OF_RECORD.md`

Must state, prominently:

- development/methods tier;
- zero Phase 4 confirmatory primaries;
- Q-L4 and Terminal C;
- exact Qwen structural/sparse/causal metrics;
- P4-P1/P2/P3 dispositions;
- Study-1 and Study-2 side-import boundaries;
- durability state and permanent deficit;
- prompt runtime limitation;
- independent-review and PI references;
- no Phase 5 authorization.

### Release manifest

Minimum fields:

```json
{
  "schema_version": 1,
  "study": "jspace-phase4",
  "terminal": "Q-L4-Terminal-C",
  "tier": "development-methods",
  "source_commit": "<exact commit>",
  "phase3_import_tag": "jspace-phase3-complete-v1",
  "registry": {"path": "...", "sha256": "...", "events": 0},
  "imports": [],
  "live_outputs": {"expected": 0, "verified": 0, "known_deficits": 1},
  "fresh_materialization": {"artifact": "...", "sha256": "..."},
  "independent_review": {"path": "...", "sha256": "..."},
  "pi_disposition": {"path": "...", "sha256": "..."},
  "tests": {},
  "papers": [],
  "forbidden_claims": [],
  "phase5_authorized": false
}
```

Do not put an unknown future commit hash inside a payload that must hash itself. Bind the exact source with either a two-stage manifest or the annotated tag object.

## 8.2 Full verification before merge

Run:

- Phase 4 full suite;
- Gemma full suite;
- OLMo full suite;
- all three live-evidence verifiers;
- import reconstruction;
- figure reconstruction;
- TeX/PDF deterministic builds;
- `git diff --check`;
- no-large-untracked-artifact audit;
- branch/remote divergence check.

Record results in:

```text
reports/PHASE4_FREEZE_VERIFICATION.json
reports/PHASE4_FREEZE_VERIFICATION.md
```

## 8.3 Commit closeout branch

Use a deliberate commit message such as:

```text
Phase 4.5: admit Study-2 methods records and close freeze governance
```

Push and fetch back. Require exact local/remote equality.

## 8.4 Merge into `interp_jspace_part2`

```bash
cd /content/labs
git fetch origin
git switch interp_jspace_part2
git pull --ff-only origin interp_jspace_part2
test -z "$(git status --short)"

git merge --no-ff interp_jspace_phase4_5 \
  -m "Merge Phase 4.5 closeout and freeze record"
```

Resolve no scientific conflict by choosing one side casually. If the target advanced, rerun the complete ancestry, registry, import, and narrative audit.

## 8.5 Post-merge verification

Repeat the full verification suite on the exact merged head. Create one final small freeze-record commit only if required to bind the merged head and post-merge test results.

## 8.6 Annotated tag

Only after the post-merge tests pass and the PI authorized the exact release:

```bash
git tag -a jspace-phase4-frozen-v1 \
  -m "J-space Phase 4 frozen: Q-L4 Terminal C development/methods release"

git push origin interp_jspace_part2
git push origin jspace-phase4-frozen-v1

git fetch origin
test "$(git rev-parse interp_jspace_part2)" = \
     "$(git rev-parse origin/interp_jspace_part2)"
test "$(git rev-list -n1 jspace-phase4-frozen-v1)" = \
     "$(git rev-parse interp_jspace_part2)"
```

## 8.7 Post-tag immutability

After the tag:

- Phase 4 registries are read-only except a separately authorized erratum event;
- no paper-analysis script writes into Phase 4 registered output paths;
- all new synthesis work lives on a new branch and namespace;
- any Phase 5 study imports Phase 4 only by tag and exact release manifest;
- Phase 4 is never “unfrozen” to make a later result easier to narrate.

---

# 9. F6: handoff to offline paper analysis

After the tag, create:

```text
reports/PHASE4_TO_PAPER_ANALYSIS_HANDOFF.md
```

It must include:

- frozen tag and commit;
- release manifest hash;
- registry hashes for Phase 2, Phase 3, Phase 4, Gemma, and OLMo;
- raw/derived Drive roots and exact logical URI mappings;
- claim ledgers and states of record;
- list of existing paper/handout sources;
- known stale paper passages to correct;
- allowed offline analyses;
- forbidden post-freeze reinterpretations;
- Phase 5 reopen router pointer;
- exact command to begin `paper_analysis.md` on a new branch.

The handoff must explicitly call out at least these stale narrative debts:

1. the current Phase 4 skeleton still describes Gemma at the Study-1 blocker rather than the Study-2 calibrated license;
2. the OLMo lineage paper still describes the SFT/DPO wedge and H6 as queued in some sections;
3. the Gemma course handout still labels the autopsy as stopped at a blocker in its earlier summary;
4. any text implying a canonical A1000 Qwen lens must be removed;
5. any text treating Bank-W as merely pending rather than capability/power blocked must be scoped precisely.

---

# 10. Review-specific scientific wording

## 10.1 Allowed Phase 4 terminal sentence

> Phase 4 found strong same-corpus convergence of the averaged Qwen transport operator, but the frozen sparse-selection and bridge-rescue invariance gates failed at A1000. The mechanical Q-L4 route therefore nominates no single sparse Qwen instrument and opens no Phase 4 confirmatory primary. Parallel Gemma and OLMo studies sharpen the checkpoint-specific validity boundary of linear transport and stage attribution, at methods/development tier only.

## 10.2 Allowed Gemma sentence

> Under a prospectively calibrated mixed-precision exact-backend envelope, the preserved Gemma Stage-1 result is a closed finite-scale tangent-mismatch measurement over the tested prompts, layers, directions, target map, and doses. It is not a nondifferentiability, missing-information, workspace, or named-mechanism claim.

## 10.3 Allowed OLMo sentence

> The first released Think transition remains associated with J-mapped dictionary and causal-use reorganization without measured sparse-capacity growth, but the official SFT/DPO wedge could not localize that transition because its prospective Bank-S capability cohorts were empty. The tested in-band H6 ladder also licenses no finite-dose transport regime at L24/L32/L40.

## 10.4 Forbidden terminal shortcuts

Do not write:

- “A1000 converged” without naming which object converged;
- “Qwen has no J-space”;
- “Gemma is nonlinear” without the exact finite-scale map and tested scope;
- “Gemma lacks a workspace”;
- “SFT/DPO show no effect”;
- “OLMo transport fails” without distinguishing H6 tangent prediction from paired ablation;
- “Bank W is negative”;
- “Phase 4 found no effect”;
- “Phase 4 was confirmatory”;
- “the historical state was recovered”;
- “all artifacts verify” without the permanent deficit;
- “Phase 5 is approved.”

---

# 11. Schedule and drop order

## 11.1 Suggested sequence

### Session A: integrated source audit

1. F0 foundation.
2. Verify all registries and bundles.
3. Implement/import tests.
4. Admit Gemma Study 2.
5. Admit OLMo Study 2.
6. Update narrative documents.
7. Push a complete admission boundary.

### Session B: fresh-materialization release proof

1. New VM/environment.
2. Fresh repository checkout.
3. Fresh Drive materialization.
4. Durability and inventory.
5. Permanent-deficit packet.
6. Push release-proof boundary.

### Session C: external review and freeze

1. Narrative-blind independent review.
2. Untouched-data audit.
3. PI disposition.
4. State of record and release manifest.
5. Full test/build reconstruction.
6. Merge and post-merge verification.
7. Tag and push.
8. Paper-analysis handoff.

## 11.2 Never-drop items

- Study-2 import verification;
- fresh materialization;
- independent review;
- permanent-deficit decision;
- PI disposition;
- full post-merge tests;
- release manifest;
- annotated tag;
- paper-analysis handoff.

## 11.3 First items to drop if time is constrained

```text
cosmetic handout redesign
additional summary figures
historical narrative polishing beyond stale-claim correction
nonessential dashboard work
```

Never drop a review or durability gate to fit presentation work.

---

# 12. Risks and responses

## 12.1 Temptation to call the fresh pass “clean”

The permanent historical deficit remains. Use `policy_aware_release_set_verified`, not a false 100% claim.

## 12.2 Late side imports appear to change the Phase 4 result

They do not. Q-L4 and the zero-primary disposition are independent. The imports update only methods/development boundaries and the paper narrative.

## 12.3 Gemma relicensing is narrated as a reversal

Preserve the chronology: Study 1 correctly failed its `1e-5` gate; Study 2 asked a new target-blind calibration question and licensed the unchanged object under a new frozen envelope.

## 12.4 OLMo missing rows become zeros in an aggregate

Require explicit missingness codes and fail any analysis that fills the SFT/DPO intervention cells with zero.

## 12.5 The 0.7788 result triggers an informal two-family patch

No. The first passing count of 18 is a Phase 5 design fact. Any new family authoring is prospective and belongs outside the frozen Phase 4 branch.

## 12.6 External review becomes a rubber stamp

Require independent reconstruction of the decisive metrics and branch router. A narrative-only review fails.

## 12.7 The freeze tag is created on the wrong head

Fetch after push and assert the tag commit equals the exact remote `interp_jspace_part2` head.

## 12.8 Paper analysis mutates frozen artifacts

The handoff must state read-only rules and use a new branch plus derived namespace.

---

# 13. Completion checklist

Phase 4.5 is complete only when all applicable boxes are checked.

## Source and imports

- [ ] exact launch HEAD and parent recorded;
- [ ] Phase 4.4, Gemma Study 2, and OLMo Study 2 ancestry verified;
- [ ] all three registries verify independently;
- [ ] Gemma Study-2 bundle and frozen prefix rehash exactly;
- [ ] OLMo Study-2 bundle and frozen prefix rehash exactly;
- [ ] `p4-import-gemma-transport-study2-v1` registered;
- [ ] `p4-import-olmo-lineage-study2-v1` registered;
- [ ] no native side ID appears in Phase 4;
- [ ] source tiers and forbidden uses preserved;
- [ ] conclusion skeleton and methods record updated without altering Q-L4.

## Durability

- [ ] fresh VM/environment used;
- [ ] Drive freshly materialized;
- [ ] every non-deficit live output verifies;
- [ ] exactly one known permanent deficit remains;
- [ ] zero unexpected deficits;
- [ ] zero wrong hashes;
- [ ] zero pin conflicts;
- [ ] policy-aware release proof generated;
- [ ] permanent-deficit packet complete.

## Review and governance

- [ ] independent reviewer/session is genuinely separate;
- [ ] narrative-blind protocol followed;
- [ ] Q-L4 reconstructed;
- [ ] untouched-data audit passes;
- [ ] Study-2 claim boundaries reviewed;
- [ ] permanent-deficit recommendation recorded;
- [ ] PI explicitly accepts or rejects every disposition item;
- [ ] freeze authorization names the exact commit.

## Release

- [ ] Phase 4 state of record created;
- [ ] release manifest created;
- [ ] claim ledger created;
- [ ] reproduction guide created;
- [ ] known-limitations file created;
- [ ] Phase 4, Gemma, and OLMo suites pass;
- [ ] live-evidence verifiers pass;
- [ ] figures and PDFs reconstruct;
- [ ] closeout branch pushed and fetched back;
- [ ] ancestry-preserving merge completed;
- [ ] post-merge verification passes;
- [ ] annotated tag `jspace-phase4-frozen-v1` points to remote mainline head;
- [ ] paper-analysis handoff created.

## Scientific boundary

- [ ] no model-scale process ran;
- [ ] no new bank/model/endpoint/SESOI exists;
- [ ] no confirmatory/replication intervention outcome opened;
- [ ] no A2000;
- [ ] no canonical sparse Qwen lens claimed;
- [ ] no Gemma workspace or named-mechanism claim;
- [ ] no SFT/DPO zero substituted for missing effects;
- [ ] no Bank-W intervention authorized;
- [ ] Phase 5 remains closed pending offline analysis.

---

# 14. Definition of done and next command

The final repository state should support this exact sentence:

> Phase 4 is frozen at Q-L4 Terminal C as a development/methods release. Its main contribution is the demonstrated separation among averaged-operator convergence, sparse-coordinate invariance, and causal-endpoint invariance. The completed Gemma and OLMo side studies are admitted at their native methods/development tiers, all current release artifacts verify from a fresh materialization except one explicitly accepted permanent historical deficit, and no Phase 4 confirmatory primary or intervention outcome was opened.

After the tag, the next agent command is not a GPU runner. It is:

```text
Read paper_analysis.md and begin the frozen-evidence synthesis on a new branch from jspace-phase4-frozen-v1.
```

