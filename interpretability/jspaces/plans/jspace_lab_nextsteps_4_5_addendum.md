# jspace_lab_nextsteps_4_5_addendum.md — Closeout Block (admit, verify, review, freeze Terminal C)

**Governs with:** `jspace_lab_nextsteps_4_5.md` (operative plan). On conflict, §2–§3 here win. The two final inprogress records (mainline VM14; sidelines block 2) are the operational state-of-record: the merged parent head `deaeb8ba…`, the registered Q-L4 functional/selection/influence chain, the prospective runtime-contract amendment, both terminal side bundles with their hashes, and the standing prohibitions (no self-signing, no confirmatory outcomes, no A2000, no new bank/model/endpoint/SESOI) all carry forward unchanged.

> **Paste-line for the closeout agent**
> Read `jspace_lab_nextsteps_4_5.md` in full, then this file, then both final inprogress records. This block is governance, not science: F0 verification → F1 dual-bundle admission → F2 fresh-materialization durability → F3 freeze packet + genuinely narrative-blind independent review → F4 PI disposition → F5 freeze record, release manifest, ancestry-preserving merge, and tag. Nothing in this block generates a new scientific number; anything that would is out of scope by definition.

## 1. Endorsements

Adopted whole: the Terminal C label and its full §0.1 table; the ruling that the Study-2 sidelines sharpen the methods boundary without reopening any primary; the second import layer with re-verification of both bundles by hash; the genuinely-fresh durability boundary and its stop rules; the freeze-packet contents; the narrative-blind review protocol; the PI-disposition gate; and §0.3's refusal to open Phase 5 in this block. Terminal C arrives pre-blessed — the Terminal-B/C pre-commitment was pushed at `6f23a29` *before* the functional gate ran, which is exactly what makes this freeze unimpeachable.

## 2. Amendments

**2.1 Elevate the runtime-identity incident to a named scientific object.** The prompt-112/323 backward-semantics block — nominally identical GPU/driver/CUDA/Torch/Transformers/Triton/FLA identities producing materially different gradients, caught by a 0.5-tolerance contract before any contribution was written, resolved by a prospective contract amendment, and closed by a negligible-influence result — is currently narrated as an operational incident. It is also a first-class methods finding about gradient-based interpretability reproducibility, and the freeze packet must carry it as one: a short registered synthesis note (incident → detection contract → diagnostics inventory → prospective resolution → scientific consequence: fit-era and current-era backward semantics cannot be assumed interchangeable under version-level pinning alone). Paper B's reviewers will consider this one of the most useful things in the campaign; do not let it live only in a block file.

**2.2 Freeze the *scoped* Q-L4 wording now.** Q-L4's honest shape is asymmetric: the averaged operator is strongly converged (0.9987 task q50), occupancy, centered excess, span-safe specificity, tail rate, G4, and bridge preference are all stable across fits — while literal selections, the selected subspace, and exactly one load-bearing causal endpoint (bridge rescue, −0.294 nat) are fit-sensitive. The freeze record's summary sentence must carry both halves: *"aggregate causal conclusions are fit-stable; sparse selection identity and one mechanism endpoint are not, so no single sparse lens is nominated."* Written this way at freeze time, the paper-analysis phase inherits the nuance instead of re-deriving it under deadline; written as bare "Q-L4," it invites over-reading in both directions.

**2.3 The one permitted GPU item.** This block is CPU/IO except, at most, the outstanding A120 capacity-reconstruction durability repair (requires ≥75 GB free, non-blocking, never a reason to delay F3–F5). If it cannot run, the durability report keeps its honest red lines — the two known historical deficits plus this one — and the freeze proceeds; item 4's archival treatment from the 4.4 resolutions stands.

**2.4 The blind review must be blind in fact.** A fresh session, given only the freeze packet, the registries, and the review checklist — no inprogress narratives, no this-conversation context, no addenda. Its output is written before any comparison with the mainline's own summaries. If scheduling pressure tempts a same-session "review," the correct move is to ship the packet unsigned and let the review happen after the block; an unsigned honest packet outranks a signed hollow one.

**2.5 Post-freeze hand-off is part of F5.** The final commit set creates the paper-analysis entry points: the analysis-branch pointer, a `FREEZE_HANDOFF.md` naming the tag, the three registry heads, both bundle hashes, the runtime-identity synthesis note, and the six skeleton sentences with their frozen tiers. The governed pre-canonical handout boundary lifts *at the tag* — handout regeneration then belongs to the analysis phase, not to this block's last hour.

## 3. PI resolutions

1. **Q-L4 acceptance:** accepted, conditional on the reviewer confirming the five-stage chain reads as registered; the mechanical router's output is not renegotiable after the fact.
2. **Terminal C:** accepted as a successful terminal per the pre-commitment; no prettification attempts.
3. **Study-2 admissions:** both bundles admitted at development/methods tier with claim boundaries quoted verbatim — Gemma's closed finite-scale result licenses no mechanism/absence claims; OLMo's `null_or_unresolved` wedge licenses no stage localization and records effects as missing-not-zero.
4. **Runtime incident:** archived per §2.1 as a named methods object; the two null states remain preserved, never reused.
5. **Signatures:** independent-review and PI fields signed only after the complete packet and the blind review's written output exist; the tag follows the signatures, never precedes them.

## 4. Risks

(i) Tag-then-tinker: after F5, the only lawful writes are on the analysis branch — treat the tag as physically read-only. (ii) Review theater is the block's one integrity risk; §2.4 is the guard. (iii) Fresh-materialization Drive quota: stage the durability pass before any large bundle copies. (iv) The temptation to "quickly check" one more scientific question with the warm GPU — the answer is the paper-analysis plan, which begins the moment this block's tag exists.
