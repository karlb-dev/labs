# Frozen G2.2 candidate sentences

Status: committed before G2.1. These are route templates, not outcomes. Bracketed fields are filled mechanically from registered artifacts; surrounding wording may only be narrowed.

## Branch 1 — calibrated floor, no recompute

> Under the prospectively calibrated `[MODEL/APPLICABLE]` backend envelope, the registered study-1 all-slot disagreement (`[HISTORICAL_ERROR]`) lies within the frozen ceiling (`[CEILING]`), while the selected scientific slot remains bit-identical. The unchanged five-layer `local_tangent_mismatch` classifier is therefore licensed as a closed exact-JVP finite-scale methods result on the tested prompts, layers, directions, target map, and doses; it is not a claim of nondifferentiability, missing information, or workspace absence.

## Branch 2 — batch-composition nuisance and declared-dose batch-1 replay

> The prospectively calibrated study-2 audit localized the cross-backend discrepancy to batch composition: the historical full-batch value remains outside the frozen batch-1 envelope, so no historical row was selected or relabeled. In the predeclared 80-row batch-1, epsilon-0.10 replay, the unchanged study-1 classifier was `[REPRODUCED_ALL_LAYERS / CHANGED_AT_LISTED_LAYERS / UNEVALUABLE_AT_LISTED_LAYERS]`; only that registered batch-1 object is licensed, and the original full-batch diagnostic remains immutable.

## Branch 3 — path ambiguity or unstable envelope

> The study-2 calibration measured a reproducible exact-backend path ambiguity that could not be bounded by a stable numerical floor under the frozen rule. Gemma Stage 1 therefore remains a methods blocker: its forward-mode mismatch is retained as a descriptive instrument output, but no exact-JVP mechanism, curvature, heterogeneity, late-band, or workspace interpretation is licensed.

## Universal ceiling sentence after a successful G2.2 route

> At the tested prompts, layers, directions, and intervention-relevant finite scales, the prompt-specific first-order tangent of the chosen source-to-target residual map predicts Gemma's finite response substantially less accurately than the same estimator predicts the OLMo control; the mismatch changes character with depth.

This final sentence may be used only if the selected route licenses it. It may not be strengthened automatically.
