# OLMo lineage study 2 — frozen SFT/DPO wedge and H6 design

Status: `FROZEN_PRE_WEDGE_MODEL_LOAD`. Tier: development/methods. Parent: `901fb4fc7578a913088c7947a2e6240f7fc45aeb`. Branch: `interp_jspace_olmo_lineage_2`. Drive root: `olmo_lineage_2_20260803`.

## Ancestry and natural-experiment boundary

The first study-2 OLMo event must be `ol2-checkpoint-ancestry-v1`. It rehashes the registered inventory-v2 artifact and records exact SFT/DPO revisions, architecture/config/tokenizer hashes, weight manifests, and model-card base-model statements. The cards identify parent repositories rather than immutable parent revisions; therefore this is an official, ancestry-qualified stage wedge, not randomized or byte-proven objective attribution. A missing field narrows the claim but does not get silently assumed.

## Frozen third-timestamp predictions

All predictions apply to both already registered frames: the Base-lens common frame and the OLMo-3 32B Think endpoint own-lens frame. The primary statistic is span-safe J minus exact instantaneous rank/energy matched control. A transition visible in only one frame is preclassified as a coordinate finding, not causal recruitment. The diffuse route is informative, not failure.

| Account | SFT span-safe specific | DPO minus SFT | Matched/random controls | Both frames |
|---|---|---|---|---|
| Transition by SFT | Think-like (`direct <= -0.08`, upper 90% bound `<= -0.04`) | no resolved negative increment | approximately zero | required |
| Transition at DPO | Base-like (`abs(direct) <= 0.04`, interval contains zero) | resolved negative increment (`<= -0.04`, upper bound `< 0`) | approximately zero | required |
| Diffuse | intermediate negative movement | additional negative movement | approximately zero | required |

Per-condition signs, capability floors, interval method, frame agreement, route order, exclusions, seed namespace, item order, and the single-lens Tier-2 trigger are frozen in `ol2_stage_wedge.yaml`. The seven conditions are baseline, span-safe J, exact instantaneous rank/energy matched control, label-protected J, protected-energy control, mechanics-random control, and logit-space label protection. Gated capability is trajectory information and is never encoded as zero.

No prompt, task bank, alias, threshold, frame, or capability rule may be repaired after a weak stage outcome. The 972-row Bank F+S G5 battery runs before interventions at each checkpoint; the capable Bank-S cohort is frozen before either frame opens intervention rows.

## H6

The transport thresholds, epsilon ladder through 0.005 and smaller measurable values, mandatory Base/3.1-Think cells, layer set L24/L32/L40/L56, dose-coverage floor, and pass/late-only/scale-limited/fail/license-held router are frozen in `ol2_transport_validation.yaml`. A verdict requires an exact registered Gemma study-2 calibration import. If Gemma finds path ambiguity, measurements may run but the license stays held. H6 never retroactively changes the wedge and never opens O5 automatically.

## Stop and tier rules

Snapshot, tokenizer, BOS, prompt-encoding, baseline replay, rank/energy, protection, seed, and cache conformance are hard gates. This study remains development/natural-experiment or methods tier regardless of sign. It cannot say that SFT or DPO objective caused workspace formation, that Instruct is a temporal successor, or that a transport result validates or invalidates the registered paired ablation effect.
