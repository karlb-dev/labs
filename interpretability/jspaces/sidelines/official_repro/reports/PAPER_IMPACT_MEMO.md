# PAPER_IMPACT_MEMO — bounded insertions the frozen paper routes MAY
# adopt (advisory only; edits nothing automatically)

Audience: the PI and the two frozen paper routes (empirical main /
methods companion). Every item is an optional, bounded insertion with
its evidence id; none reopens a frozen claim.

## For the methods companion

1. **Route E sentence (ready to lift):** "On a frozen released-prompt
   subset, targeted two-coordinate substitution and broad protected
   J-suppression dissociate: the ablation is selectively destructive
   (14/30 vs 1/30 matched on Qwen; 5–6/19 vs 1/19 on OLMo) while
   paper-literal swaps rarely complete the flip — the two interventions
   estimate different causal questions." (or1-instrument-crossover-*)
2. **Instrument-basis footnote:** the g-folding materiality asymmetry
   (Qwen 0.382 vs OLMo 0.9886) as a concrete example of why
   version-level "same lens" language under-specifies the instrument
   (extends the campaign's C6/C7 line). (D9; admission events)
3. **Fit-economics datapoint:** the official estimator at n=250
   (2×125, 161 s/prompt, dim_batch 16 on a 96GB part) is fit-stable
   with halves ≤0.015 eval gap — evidence the n=1000 recipe is a
   ceiling, not a requirement, consistent with upstream's own
   "quality saturates quickly." (or1-olmo-fit-*)

## For the empirical main (external-context paragraph)

4. **Population moderation (Route F):** the campaign's protected-
   ablation selectivity reappears on the official prompt population on
   both lanes — the C1 result is not campaign-prompt-specific.
   (or1-instrument-crossover-*)
5. **Cross-model boundary sentence:** the J-lens readout advantage
   itself is model-dependent (present on Qwen, absent on OLMo whose
   in-band average Jacobians are near identity) — useful when
   discussing which models a workspace-style analysis presupposes.
   (or1-{qwen,olmo}-lens-evals-v1)

## Future-work candidates surfaced (not Study-2 authorizations)

- α-response curves (0<α<2 fine grid) to localize the open-model
  overshoot the α=2 inversion implies.
- A boundary-form tokenization contract for report-style scoring
  (or1-001's lesson) as a methods note.
- An introspection ladder calibrated per model family before
  concluding on report-channel absence (our reconstructed ladder may
  simply be mis-scaled for open models).

Nothing above may be pasted into a frozen route without PI sign-off;
this memo is the study's complete export surface to the papers.
