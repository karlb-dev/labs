# jspace_lab_nextsteps_2_2_addendum.md — Verification, PI Decisions, and Amendments

**Reviews:** `jspace_lab_nextsteps_2_2.md` (the forensic branch review), `SCIENTIFIC_PREREGISTRATION_DRAFT.md` (v2), `inprogress.md` (end of VM7), the Part 2 living handout, and branch `interp_jspace_part2` @ `d4f2c69` vs `main` @ `4097c44`.
**Role of this file:** independent verification of the review's decisive claims, the principal investigator's resolutions of decisions D1–D6 (plus a new D7), amendments and additions to the nextsteps plan, and the merged execution order. Where this file and `nextsteps_2_2` conflict, the PI-decisions section here (§3) governs; everywhere else, `nextsteps_2_2` remains the operative repair spec.

> **Paste-line for the coding/research agent**
> Read `jspace_lab_nextsteps_2_2.md` in full, then this file. Execute the nextsteps stages N0–N8 in order with the amendments in §4 below and the PI decisions in §3 treated as approved-for-candidate: implement the supporting analyses, build `SCIENTIFIC_PREREGISTRATION_CANDIDATE.md` around them, and STOP before the binding freeze commit for final PI sign-off exactly as nextsteps §7-N5 specifies. Everything you produce before the freeze remains pilot/dev/methods tier in the registry. Preserve the withdrawal-and-supersession discipline and the standing contradiction heuristic — they are working.

---

## 1. Verification results (what was independently checked)

**Code claims in nextsteps_2_2 — 4/4 confirmed at `d4f2c69`:**
1. `battery.py:55` derives the clustering family as `it["name"].split("-")[0]`. Confirmed. Every published paired CI, ICC, tail-rate interval, and the G6 power simulation inherit this field. P0 status upheld.
2. `occupancy.py` computes `hc = h - global_mean` (line 115) and then computes `share_j`/`share_r` from raw `(h*h)` energy (117–118); the in-code comment honestly defers the centered share, but report/handout prose calls the result the paper's centered excess variance. Confirmed. The occupancy-crossing number and the excess-share number are different estimands fused under one label.
3. `protected_dynamic.py:108` sets `take = min(k, avail.min())` — a single low-availability row shrinks the dose for every row. Confirmed. The `pad by repeating last row` guard (line 93) corroborates the prefill-broadcast concern for the generation path.
4. The generation-path protection-set misalignment and logging-semantics issues are consistent with the code inspected; treat as confirmed pending the v2 rewrite's golden tests.

**Standing verifications carried forward from the previous review cycle:** the paper's ablation protection clause, occupancy definition (marginal-gain crossing vs random controls; excess variance at median occupancy), and broadcast assay (MLP gain; OV gain + label preservation; J-rotated control) were all verified against the paper text directly. The R7/R2 pilot instruments are therefore aimed at the right estimands in intent; the remaining gaps are the implementation items above.

**Consistency checks across the four documents:** the prereg draft, inprogress, handout, and nextsteps agree on the pilot facts (protection flips the median to ~0; the 16/60 protected tail with 13 protected-answer members; occupancy 2 vs 3–4 and excess ≤0.9% vs 1.0–2.2%; the four-model ladder REVERSE/EQUAL/WEAK-FWD/CLEAN-FWD; the easy-fact relabeling of base; the endpoint-dependent Think/Instruct swap; Gemma's linear-premise failure and L22 non-identification; B3 frozen-logit at ~53%; A0 transfer shortlist-yes/rank-1-no). No contradictions found between documents. One stale item: the prereg's HP1 frozen wording ("base ≈ 0 < Think ≈ 0 < Instruct < Qwen") is both internally incoherent as an ordering and stale relative to the ladder + ceiling check; nextsteps §5 already supersedes it — adopt that wording.

## 2. Progress verdict

The branch is the strongest 48 hours of the whole campaign. Beyond the engineering (registry, provenance, protected assay, positive control, hard-item bank), the *science* moved: the central Part-1 interpretation was overturned by the repaired instrument (live collapse = output-stream deletion), the capacity claim was rebuilt on the paper's own estimator and survived in sharper form (all open models an order of magnitude below Claude's band, fit size ruled out via the published n=1000 Qwen lens), the causal signature was shown to reorganize non-monotonically along post-training and to decouple from measured occupancy, the tail acquired a mechanism ("deletion costs only when the answer is not locked in") with a falsifier, and two genuinely novel methods results landed — the linearization-faithfulness measurement (~0.49 cosine in the paper's own band on the model where the method works) and the Gemma linear-transport-premise failure with the L22 identification gate. Seven withdrawn analyses, each caught by internal contradiction, is the health indicator that matters most. The correct one-line status is nextsteps' own: *repair era scientifically productive; freeze blocked by a small number of identifiable correctness repairs.*

## 3. PI decisions — resolved for candidate preparation

These are the resolutions to build the preregistration candidate around. The binding freeze still requires explicit sign-off on the finished candidate (nextsteps §7-N5).

**D1 — endpoint: adopt the split strategy (nextsteps §4-D1), with one amendment.** Binary primaries on the paired tail-rate endpoint for HP2/HP3 at the frozen −1.0-nat threshold; HP1 becomes a continuous model-by-task interaction with the prespecified 3.1-Think-vs-3.1-Instruct contrast, estimation-with-CI, no four-model rank claim; hurdle-style two-part model as prespecified secondary; both endpoints reported side by side with the pilot Think/Instruct swap disclosed as the reason. **Amendment (tail stratification):** the primary HP3 tail must be defined on *protected-answer items only* (clean answer rank ≤ protect_k), with the all-items tail as prespecified sensitivity. The mechanism result showed 21% of pilot tail items were protection failures — items whose answers were unprotectable — and an unconditional tail conflates indirect J-content damage with instrument reach. The stratified endpoint is the claim we actually want to make.

**D2 — lens: run the N3 context-J study before freeze; mean-J remains the replication primary regardless of outcome.** Adopt nextsteps' resolution and framing (`meanJ_paper` primary; `contextJ_methods` arm gated on dev-only thresholds committed before the run). Two amendments. (a) **Add a decomposition arm:** three estimators, not two — (i) exact per-context per-position J via JVP/VJP, (ii) per-position J averaged over prompts only, (iii) the campaign lens (averaged over both). The i→ii gap isolates prompt-averaging loss and ii→iii isolates position-averaging loss; if most accuracy loss is positional, a per-position corpus-averaged dictionary is a practical middle estimator that stays context-general. (b) **Framing for the paper:** the averaged Jacobian is not merely a degraded estimator — the paper argues the averaging is what defines *verbalizable* (disposition across contexts) as opposed to *verbalized here*. So report the faithfulness gap as the measured price of the construct, and note that it bounds the causal fidelity of every J-direction intervention in this literature, the original paper's included. That sentence is earned and should survive to the final draft.

**D3 — Gemma: option (a).** Methods boundary case; no confirmatory J-causal cells; keep the fit-identification, depth-opacity, linearity, and norm-convention findings. The queued pre-cap-target and target-layer-sweep variants stay queued behind everything confirmatory and run only on leftover compute. The honest wording ceiling stands: "the paper's method as implemented does not transfer to this architecture," never "Gemma has no workspace" — and per nextsteps §3.4, narrow the linearity wording to tested finite scales.

**D4 — tail threshold: freeze −1.0 nat**, with prespecified sensitivity at −0.5/−1.5/−2.0 and no threshold shopping. The threshold's arbitrariness is disclosed with the sensitivity curves already banked.

**D5 — item bank: expand, then family-split.** Nextsteps' preferred route: grow to ≥60 canonical families (target 70) with ≥3 capable items each, authored in the empirically productive shapes from the c3-v2/v3 scoring (classification/category relations, second-order superlatives, competing-alternative sets, derived units, numerics — and explicitly *not* famous-proper-noun recall, which went 0/16). Deterministic stratified family-level split into confirmatory/replication with the full manifest and hashes; canonical-family map applied before counting anything. Authoring is codegen-cheap and scoring is GPU-minutes, so there is no compute case for the fallback.

**D6 — G5: run it.** No waiver. The immutable item manifest, bridge-swap counterfactual gate for every two-hop item, per-model capability cohorts (lineage-anchor / cross-model-intersection / model-specific), and shortcut audits are all as specified in nextsteps §2.6/§7-N4. Emphasis: the capability-cohort design is what makes HP1's Qwen external-validation contrast interpretable — without it, a bank selected on OLMo difficulty smuggles a sampling bias into every cross-model claim.

**D7 (new) — publication packaging: one paper for now, methods sections written extractable.** The faithfulness measurement + Gemma boundary + identification gate could ship today as a standalone methods note ("How faithful is the Jacobian lens as a causal model?") independent of any freeze. Resolution: do not split yet — the methods results are load-bearing context for the main matrix paper and splitting halves the attention both halves get — but write those sections self-contained so the split remains a one-day decision if the confirmatory campaign stalls. Revisit at N8.

## 4. Amendments and additions to nextsteps_2_2

1. **Repro-contract scope (amends §2.4/§7-N1.3).** Implement the event-sourced registry, payload/provenance hash split, constraints lock, logical URIs, and model-revision manifests in full — they're cheap and they close real holes (the timestamp-in-hashed-file defect alone justifies the split). Scope the "fresh-environment demonstration" to exactly two acceptance items (one CPU evidence item + the SmolLM golden) rather than universal re-executability; container digests are deferred. This preserves the PI's binding reproducibility directive at solo-lab cost.
2. **Family-map repair sequencing note (amends §2.1).** After recomputation, expect the design numbers to move: the G6 power sim, the ICC, and possibly the ladder ordering all inherit the corrected clusters — and the endpoint choice (D1) was itself calibrated on the old clusters. Therefore the D1 numbers in the candidate must be regenerated from post-repair evidence IDs, and if the corrected clusters reorder Think/Instruct *again*, report exactly that and keep HP1 as interaction+contrast (which is robust to reordering by construction).
3. **A2a Qwen thinking-on/off retains its slot (amends §15).** Nextsteps' priority list drops A2a below the fold. Keep it immediately after the primary pair and Qwen validation cells, as the campaign order in `inprogress.md` already has it: it is the cleanest H1b test in existence (same weights, official mode toggle) and the register-confound discovery already dictates its endpoint design (generation-based only, frozen extractor and budget). It is one model-load plus a modest grid.
4. **G4 positive control is per-model (amends §7-N6).** The swap-injection positive control (0.76 vs 0.18) must rerun on each confirmatory checkpoint with its own finalized lens before that model's primary cells count. A null on a model whose positive control fails is assay failure, not evidence — this is already the addendum-lineage rule; make it an explicit N6 stop-rule line item.
5. **HP4 scoping (amends §5-HP4).** The capacity-boundary hypothesis must scope to "models satisfying the J-lens validity premises (linear transport at intervention scales; cross-corpus identification)" — Gemma is excluded by premise failure, not counted as a below-boundary data point. Also carry the right-censoring and solver-sensitivity reporting exactly as specified.
6. **Tail-endpoint reporting addition.** Alongside the stratified primary (D1 amendment), prereg one descriptive secondary: the protected-tail *mechanism profile* (bridge-deflation rate, protection-failure rate, clean-rank distribution of tail vs non-tail) on the confirmatory partition, since the pilot version of this table is the single most interpretable exhibit the campaign has produced and its replication is itself informative.
7. **Report/handout hygiene (endorses §11, adds one item).** Every figure gets the PILOT/CONFIRMATORY watermark now, before anything circulates further; and the resume read-order in `inprogress.md` should insert `jspace_lab_nextsteps_2_2.md` and this addendum immediately after the governing Part-2 addendum, with the standing-debts section absorbing nextsteps' P0 list verbatim so a fresh agent cannot miss them.
8. **Keep the contradiction heuristic as a named standing rule.** Three instrument faults in one block were each caught because a new measurement contradicted an established result and the instrument was suspected first. Write it into the package README as lab law; it is the campaign's real edge.

## 5. Risks and compute sketch

Chief risks, in order: (i) family repair widens the pilot CIs enough that the tail-rate endpoint also becomes underpowered at the planned n — mitigation is the D5 expansion, which grows independent families, the quantity that actually buys power; (ii) the 3.1-Think lens does not exist yet (pilot Think cells used 3.0) — budget the fit (~5 h at Part-1 rates) and do not let the historical 3.0 cell impersonate the primary; (iii) the corrected R2 centered-variance number moves the capacity headline — acceptable, that is what erratum discipline is for; (iv) freeze-then-discover — mitigated by running D2/N3 and G5 *before* candidate assembly as ordered.

Rough remaining envelope at Part-1/2 rates on a 96 GB VM: N1 ≈ 0.5 block CPU-dominant + tiny-model GPU; N2 ≈ 2–4 GPU h (occupancy reruns); N3 ≈ 1–3 h; N4 ≈ 1–2 h scoring + CPU authoring; N5 CPU; N6 ≈ 12–18 h including the 3.1-Think lens fit and both primary-pair grids plus Qwen validation; N7 ≈ 4–6 h; N8 CPU + one fresh-VM demonstration. Total ≈ 25–35 GPU hours — comfortably two 24 h VM blocks with margin.

## 6. Bottom line

The campaign has done the rare thing twice now: it has let its own instruments overrule its own best story, and it has kept the receipts. The remaining path to a paper that survives review is short and fully specified: repair the family units and the two estimand mismatches, make the reproduction contract real at honest scope, run D2 and G5, expand and split the bank, freeze, and then run the primary pair once, cleanly. Every one of the five outcome branches in nextsteps §13 is publishable from there. Do not add a single new model, dose, or dramatic bar before the freeze.
