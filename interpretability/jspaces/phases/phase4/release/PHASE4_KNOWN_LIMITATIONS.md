# Phase 4 known limitations

**EXPLICIT RELEASE LIMITATIONS — CARRIED, NOT AVERAGED AWAY**

Frozen 2026-08-04 with tag `jspace-phase4-frozen-v1`.

1. **One permanent historical deficit.** The A120–A250 operational
   `state.json` (SHA-256 `361bda08e9ffbe1d...f45e8`) is permanently absent
   after an exhausted exact-copy search. The event
   `p4-qwen-multilens-functional-gate-a120-a250-published-dev-v1` is
   therefore partially durable with its decision role superseded by the
   later live functional gates. Sixteen of its seventeen outputs verify,
   including the exactly recovered A120 capacity tensor. Whole-registry
   durability honestly reports 520/521; "all artifacts verify" is never
   claimed without this exception. Reviewer and PI accepted release with
   this limitation (`reviews/PHASE4_PERMANENT_DEFICIT_REVIEW_PACKET.md`).

2. **Prompt-influence runtime scope.** The prompt-323 influence result is a
   current-runtime sensitivity shape under the prospective amendment; the
   historical fit-era backward runtime could not be reconstructed because
   distribution-content and compiled-kernel identities were not preserved
   at fit time. No historical-runtime reproducibility claim is made
   (`p4-runtime-identity-synthesis-v1`).

3. **Q-L4 asymmetry.** The averaged transport operator is strongly
   converged on the registered assay, but literal sparse selections, the
   selected subspace, and the bridge-rescue causal endpoint are
   fit-sensitive. No single sparse Qwen lens is nominated; consumers must
   not cite a canonical A1000 lens.

4. **Capability-gated stage attribution.** The official Think-SFT/DPO wedge
   has empty prospective Bank-S cohorts; stage effects are missing, not
   zero. No SFT/DPO localization exists in either direction.

5. **Transport gating is checkpoint- and scale-specific.** Gemma's
   five-layer classifier is a closed finite-scale methods result over its
   exact tested scope only; the OLMo H6 ladder licenses no in-band
   finite-dose regime at L24/L32/L40, with a Think-only L56 epsilon-0.10
   late anchor. Neither bounds the registered paired projection-ablation
   effects, whose in-situ validity evidence is their matched-control and
   positive-control behavior.

6. **Dose-archive schema gap.** The exact per-site dose records needed to
   map registered causal interventions onto the H6 ladder were never
   collected; coverage is unavailable, not 0%. Recollecting it is a new
   prospective experiment.

7. **Bank-W pair design is unpowered at current support.** 0.7788 at 16
   shared capable families against the 0.80 target; 18 is the first
   simulated passing count. This is a planning boundary, not an
   externalization null; no intervention was opened.

8. **Registered-path portability.** A minority of registered references
   resolve via strict non-literal modes (3 identical tracked repository
   files for worktree-absolute paths; 2 exact append-only registry
   prefixes at registering commits). The durability verifier records these
   modes explicitly; hashes are never relaxed.

9. **Development cohorts throughout.** Every Phase 4 number uses known
   Phase 3 banks and development cohorts. Nothing in Phase 4 is
   confirmatory; the confirmatory/replication anchor lives in Phases 2–3.
