# Phase 4 claim ledger

**MAXIMUM LICENSED WORDING — ANYTHING STRONGER EXCEEDS THE RECORD**

Frozen 2026-08-04 with tag `jspace-phase4-frozen-v1`. Every row cites
registry events, never prose. Side-study rows are additionally bounded by
`jspace_gemma/release/gemma_transport_claim_ledger_v2.md` and
`jspace_olmo_lineage/reports/OLMO_LINEAGE_CLAIMS_TABLE_V2.md`.

## Allowed terminal sentences

### Phase 4 terminal

> Phase 4 found strong same-corpus convergence of the averaged Qwen
> transport operator, but the frozen sparse-selection and bridge-rescue
> invariance gates failed at A1000. The mechanical Q-L4 route therefore
> nominates no single sparse Qwen instrument and opens no Phase 4
> confirmatory primary. Parallel Gemma and OLMo studies sharpen the
> checkpoint-specific validity boundary of linear transport and stage
> attribution, at methods/development tier only.

License: `p4-qwen-lens-convergence-drawA-n500-n1000-dev-v1`,
`p4-qwen-multilens-functional-gate-a500-a1000-published-dev-v1`,
`p4-qwen-selection-margin-a500-a1000-dev-v1`,
`p4-qwen-lens-influence-prompt323-dev-v1`,
`p4-qwen-canonical-lens-decision-a1000-dev-v1`.

### Scoped Q-L4

> Aggregate causal conclusions are fit-stable; sparse selection identity and
> one mechanism endpoint are not, so no single sparse lens is nominated.

### Gemma

> Under a prospectively calibrated mixed-precision exact-backend envelope,
> the preserved Gemma Stage-1 result is a closed finite-scale
> tangent-mismatch measurement over the tested prompts, layers, directions,
> target map, and doses. It is not a nondifferentiability,
> missing-information, workspace, or named-mechanism claim.

License: `p4-import-gemma-transport-v1`,
`p4-import-gemma-transport-study2-v1`.

### OLMo

> The first released Think transition remains associated with J-mapped
> dictionary and causal-use reorganization without measured sparse-capacity
> growth, but the official SFT/DPO wedge could not localize that transition
> because its prospective Bank-S capability cohorts were empty. The tested
> in-band H6 ladder also licenses no finite-dose transport regime at
> L24/L32/L40.

License: `p4-import-olmo-lineage-final-v1`,
`p4-import-olmo-lineage-study2-v1`,
`p4-import-olmo-bank-w-capability-v1`,
`p4-bank-w-capability-joint-imported-dev-v1`.

### Runtime identity

> Fit-era and current-era backward semantics cannot be assumed
> interchangeable under version-level pinning alone; gradient-based
> interpretability pipelines need distribution-content and compiled-kernel
> identity plus a prospective runtime control with a frozen tolerance.

License: `p4-runtime-identity-synthesis-v1`,
`p4-qwen-lens-influence-prompt323-dev-v1`.

## Forbidden terminal shortcuts

Do not write:

- "A1000 converged" without naming which object converged;
- "Qwen has no J-space";
- "Gemma is nonlinear" without the exact finite-scale map and tested scope;
- "Gemma lacks a workspace";
- "SFT/DPO show no effect";
- "OLMo transport fails" without distinguishing H6 tangent prediction from
  paired ablation;
- "Bank W is negative";
- "Phase 4 found no effect";
- "Phase 4 was confirmatory";
- "the historical state was recovered";
- "all artifacts verify" without the permanent deficit;
- "Phase 5 is approved."

## Data-state doctrine

Missing, capability-gated, and not-applicable are data states, not zeros:
the SFT/DPO intervention cells are missing (empty prospective cohorts); the
registered-dose coverage is unavailable (archive schema), not 0%; P4-P2 and
the Bank-B orthogonal shot are not-applicable under Q-L4, not failures. Any
analysis that fills these cells with zero must fail.
