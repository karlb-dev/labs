# jspace_lab_nextsteps_4_1_addendum.md — Verification, PI Resolutions, and the Merged Schedule

**Reviews:** `jspace_lab_nextsteps_4_1.md`, `inprogress.md` (end of VM10), `REPORT_PHASE3.md`, the Phase 3 handout, and branch `interp_jspace_part2` @ `09f9872` (base lens banked).
**Role:** independent verification of the review's five release-audit claims, the PI's resolutions, amendments, and the merged execution schedule that reconciles the review's audit-first mandate with the inprogress NEXT-VM queue. Where documents conflict: `nextsteps_4_1` is the operative Phase 4 spec; §3–§5 here govern on conflicts; the inprogress NEXT-VM queue is **superseded** by §5 below (its items survive, reordered under Phase 4 governance).

> **Paste-line for the coding/research agent**
> Read `jspace_lab_nextsteps_4_1.md` in full, then this file. Phase 2 and the frozen Phase 3 confirmatory/replication artifacts are immutable. Execute the §5 merged schedule: the Phase 3 release audit's GPU pieces run FIRST on the next VM, the OLMo lineage leg fills the remainder of that block as Phase 4 Workstream A development, and no Phase 4 confirmatory cell runs before the Phase 4 freeze. Register every audit outcome as a new event; supersede, never edit. Stop for PI sign-off at the Phase 4 preregistration candidate.

---

## 1. Verification (all five release-audit claims confirmed at `09f9872`)

1. **P3-P2 stratum mismatch** — `phase3_locked_analysis.py:94–95` takes every Qwen row; no clean-rank filter exists and the primary grid stores no `clean_first_rank`. The frozen prereg's protected-answer stratum (which originated as this reviewer's own D1 amendment in the 2_2 cycle — the prereg adopted it; the code never implemented it) was not applied. The published +0.096 is the **all-items** span-safe specificity estimate. The audit's baseline-only rank recomputation and its three-way decision rule are exactly right; note the stratum version could land either direction, since protection-failure items were the original rationale for stratifying.
2. **Unstable control seeds** — `abs(hash(iid))` in `phase3_primary_grid.py:208`, `span_audit.py:187`, `prose_control_grid.py:196`; `PYTHONHASHSEED` unset. Baseline and J arms are unaffected (deterministic); only the matched-control realization varies. The seed-ensemble audit, not salt archaeology, is the correct response.
3. **Mislabeled interval** — `phase3_results_figure.py:59–60` builds `estimate ± 1.96·se` and labels it a wild-cluster 95% CI. The p=0.062 randomization test remains the declared decision ruler; the exact 2^17 enumeration and randomization-inverted confidence set are the right closeout.
4. **N8 scope** — `n8_level2_sentinels.py:86` relaunches `jspace_part2.experiments.confirmatory_protected_grid`. Levels 2–3 reproduce Phase 2 cells; rename to N8-P2-L*, and the new N8-P3 ladder becomes the actual Phase 3 release gate.
5. **Replication tier constant** — `TIER = "phase3-confirmatory"` regardless of `--side`; event-sourced metadata correction, not row rewriting.

Cumulative reviewer record across four cycles: every checkable claim verified. Treat its Part I as binding.

## 2. Phase 3 verdict

Phase 3 did its job and the results are better than the plan dared write down. The crown jewel: **the J-specific tail survives full span-safe protection** at confirmatory tier and replicates held-out (+0.096/+0.102, p≈1e-5) — the campaign's central specificity claim now rests on the leak-proof arm, at ~1/3 the label-protected size exactly as the dev decomposition predicted, which is what an honest decomposition looks like when it works. Bridge mediation confirmed on the magnitude-blind-chosen model (+0.431). P3-P1 directional under disclosed power (0.062) — and §9.4's "no post-hoc rescue" rule is adopted verbatim; the near-miss stays a near-miss. Two results that *reduce* the claims are equally load-bearing: the Bank-S null on Qwen triggered the pre-stated rule and the honest noun is now **knowledge-access channel**, and the prose exact-control shows **no model earns "selective" wording** — prose damage exceeds task damage in standardized units on all three, with the vulnerability content-specific and predominantly output-span leakage. The paper's spine is therefore: output protection separates report from computation; span-safety separates content from leak; exact dose controls isolate direction content; and what lives in the channel differs by model — bridge-routing on Qwen, accessibility/output-adjacency on OLMo. The five audit items gate the *numbers*, not the story; all are repairable, and several (the stratum conformance audit especially) will make the paper stronger for having been caught in-house.

## 3. Amendments to nextsteps_4_1

1. **Schedule reconciliation (the one real conflict).** The inprogress NEXT-VM queue continues inside `jspace_phase3` (Think-3.0 lineage leg, then mode/W/fit-size/Gemma). The review demands audit-first plus a new namespace. Resolution: the audit's GPU pieces are small (protected-answer audit ≈1–1.5 h of baseline-only Qwen logits; seed-audit subset ≈2–3 h) and run **first** on the next block because they gate the paper; the lineage leg (Think-3.0 + base dev cells + trajectory figure) is *already* Phase 4 Workstream A development per §10.2 and fills the remainder of the same block, registered under phase4-development tier via the import layer once the scaffold exists (interim: phase3-development with a migration note). The queue's items (a)–(d) survive but move behind Phase 4 development gates — no more untiered continuation.
2. **Think Bank-S anomaly, sharpened.** It is the only CI-clean *positive* composition term in the entire campaign (+0.15 [+0.04,+0.26]) and, if it survives the §10.7 forensic decomposition, it is a direct descendant of the Part 2 externalization hypothesis: the composed Bank-S prompt *is* an external workspace, and in-context state may substitute for the internal channel on the one model trained to externalize. Preregisterable prediction if it survives: the effect grows with in-prompt state redundancy and vanishes when Bank W's "internally derived" axis forces internal composition. This couples §10.7 to Workstream D — design Bank W's redundancy/derivation axes so they adjudicate the anomaly, not just the load question.
3. **P4-P3 capability guard.** The load interaction is only interpretable if baseline capability is flat (or explicitly modeled) across load levels per model — otherwise "J-dependence rises with load" is confounded with "hard items are fragile," the exact ghost the accessibility result warns about. §13.4's gates should state a baseline-accuracy-by-load flatness criterion or a covariate commitment before freeze.
4. **Bridge-swap endpoint, one addition.** Alongside the counterfactual-preference statistic, log the *generation* trichotomy (original / counterfactual / other-invalid) as a named secondary — a preference shift with rising "other" is disruption wearing a swap costume, and the trichotomy is free at scoring time. §11.4's controls otherwise stand as written.
5. **Two-track publication decision (new D-item for the PI).** After Stages 1–2 (audit + N8-P3), the Phase 2+3 manuscript is releasable on its own: frozen, replicated, audited, independently reproduced. Options: (a) hold everything for the full Phase 4 spine (~4–5 more blocks) and publish one mechanism paper; (b) submit/post the Phase 2+3 core after Stage 2 and let Phase 4 be paper two. Recommendation: draft to submission-ready during Phase 4 development regardless, then decide after P4-P1/P4-P2 land — those two results, if positive, upgrade the single paper enough to justify holding; if either stalls, release the core rather than aging it. Default under time pressure: (b).
6. **Everything else in Part II and Part III is endorsed without amendment** — the five-organization framing (§8), the compact P4-P1/P2/P3 family, lineage-as-estimation-first, the stable-seed contract with the AST test, the scoring contract, the boundary-safe alias matcher with the 100+100 hand audit, the cohort-population sensitivity quartet, the Gemma stop rules, and the small-cluster statistics upgrades.

## 4. PI resolutions

R1 — Execute the Part I audit exactly as specified; adopt each decision rule as written, including the possibility that P3-P2 narrows to the all-items claim. R2 — Phase 4 primary family = P4-P1/P2/P3 as specified, with §3.3–3.4 riders; lineage promoted to the family only under §9.3's pre-outcome conditions. R3 — Namespace/branch/tiers per §9.1; the registry rejects new Phase 2/3 evidence. R4 — Gemma runs last, two-block cap, methods tier, exact-JVP gate before anything more. R5 — Publication per §3.5: draft now, decide after P4-P1/P2. R6 — The a+bε identification fit, vector-defect homogeneity statistic, and odd-symmetry test from the Gemma case-study handout are the required estimators in the §16 autopsy.

## 5. Merged execution schedule (24 h blocks, 96 GB class)

**Parallel CPU track (starts now):** Stage-0 snapshot + `PHASE3_FREEZE_RECORD.md` + audit plan; `p3_inference_audit` (exact 2^17 enumeration is CPU); N8-P3-L1 blind analysis reproduction in a separate session; Phase 4 scaffold (`seeds.py`, `scoring4.py`, conformance gates); Bank B and Bank W authoring + source verification; paper sections 4–5 from frozen material.

**Block A (next VM):** [GPU, in order] (1) `p3_protected_answer_audit` — Qwen clean logits, confirmatory + replication (~1–1.5 h); (2) `p3_control_seed_audit` subset, 5 seeds (~2–3 h); (3) lineage leg: Think-3.0 download under stall guard → G5 → span-safe dev grid (~4–6 h); (4) base weights → G5 → base dev cell with the banked base lens (~3–4 h); (5) trajectory figure p3f06 + common-lens reruns if time. Boundary rituals throughout; audit outcomes registered before the lineage numbers are interpreted.

**Block B:** N8-P3-L2 stable-seed sentinels ×3 models + one full Phase 3 Qwen cell under the repaired runner (~6–9 h) → `jspace-phase3-complete-v1` tag → cut `interp_jspace_phase4` → bridge geometry audit + swap-target endpoint development on Qwen (~3–4 h) → mode-factorial parser/phase-hook gates (~1 h GPU + CPU).

**Block C:** Phase 4 freeze (dev gates → candidate → PI sign-off → freeze commit) → P4-P1 bridge bank confirmatory cells + P4-P2 mode-factorial primary cell (~8–12 h).

**Block D:** Bank W across the OLMo pair + Qwen (P4-P3, ~4–6 h) → Qwen nested fit-size study (~4–5 h) → receiver/broadcast slice on the strongest bridge families (~2–3 h).

**Block E (strong set / release):** Gemma autopsy block 1 (exact JVP ladder with the a+bε fit, localization) → Phase 4 independent reproduction → manuscript lock and artifact release.

Estimated: audit + true Phase 3 reproduction ≈ 12–16 GPU h; Phase 4 core (Blocks B–D) ≈ 28–38 h; strong set ≈ +8–14. Drop order under pressure: Gemma → receiver localization → fit-size beyond the Qwen nest → lineage intermediates; never drop the audit, N8-P3, P4-P1, or Bank W.

## 6. Risks

(i) The protected-answer audit may shrink P3-P2 — the decision rule already prices this in; the all-items claim stands regardless and the transparency is a credibility asset. (ii) The seed audit's DECISION-SENSITIVE branch is unlikely (baseline/J arms are deterministic; only control realizations vary) but if hit, the affected claim downgrades per the gate — do not argue with it. (iii) Bank W is the last exit for any working-memory language in the paper; a null load interaction finalizes "knowledge-access channel" as the noun, and that must be acceptable going in. (iv) The lineage leg must not cannibalize the audit window — the audit's GPU pieces run first, unconditionally. (v) Keep the contradiction heuristic: it has caught an instrument fault in every block that produced one.

## 7. Bottom line

Phase 3 ended with the strongest possible configuration: the central claim standing on the leak-proof instrument, the weaker claims honestly demoted by pre-stated rules, and a reviewer who found five real release defects *after* the freeze — every one repairable by audit rather than retraction. Phase 4's question is the right next question: not whether the channel exists, but what occupies it, per model, per training stage, per inference mode, and where the instrument itself is licensed. Run the audit, cut the tag, freeze small, and go find out.
