# jspace_lab_nextsteps_4_3_addendum.md — Mainline Phase 4 (Qwen continuation, freeze path)

**Governs with:** `jspace_lab_nextsteps_4_3.md` (operative plan). On conflict, this addendum's §2–§4 win.
**Supersession:** `inprogress (VM12)` and `resume-phase-4-2.md` are superseded *as plans* by 4_3 + this addendum, but remain the binding **operational state-of-record** for restart mechanics: the recovery contract, checkpoint hashes, lock/monitor paths, duplicate-fitter checks, and the hard governance boundary (no confirmatory/replication outcomes, no self-signed reviewer/PI fields, immutable registry, no CPU model fallback). The following rules from those documents carry forward verbatim into 4.3: never register a partial pseudo-milestone; never launch a duplicate fitter without the process/lock/GPU/log/recovery-header check; do not delete the Qwen cache while A1000 is active; DriveFS durability must be re-verified before the whole-registry output verification counts.

> **Paste-line for the mainline agent**
> Read `jspace_lab_nextsteps_4_3.md` in full, then this file, then the VM12 `inprogress`/`resume` pair for operational state. Let A1000 own the machine per 4_3 §3. Execute the §2 parallel-branch contract exactly — the side tracks live on their own branches and namespaces; this VM never writes `ol-`/`gm-` events. Draft the Q-L2 estimand amendment (below) *before* any A1000 comparison opens. Stop at the freeze candidate for independent review + PI sign-off.

## 1. Endorsements

The 4.3 plan is adopted whole: the four-outcome A1000 branch table (Q-L1..Q-L4) with frozen successor configs; the asymmetric freeze-blocker resolution (P4-P3 primary; P4-P2 conditional on the consumed-family variance pilot; P4-P1 defaulting to estimation-only P4-E1 unless the orthogonalized estimand passes its variance gate); "A1000 is the last automatic fit-size escalation"; the §2 branch/namespace/registry/import-bundle contract; and the §3–§4 process discipline for the fit hand-off.

One explicit override of the PI's stated intent: the three tracks should **not** run on one shared branch. Three branches cut from `3b04173` with path-disjoint namespaces and prefixed registries achieve exactly the stated goal (same starting point, clean isolation) without registry merge conflicts or push races; the A500 preparation-branch merge already proved the ancestry-preserving reintegration pattern works. Adopt 4_3 §2 as written.

## 2. The structural–function split is a result, not a nuisance

The A250/A500 gate produced the campaign's second major methods finding (after the transport gate): **operator convergence ≠ selection convergence ≠ causal invariance.** Matrix cosines ≥0.9977 in the assay band while selected-ID Jaccard sits at 0.54, projector overlap at 0.70, and bridge rescue moves 0.56 nats — with occupancy, capacity, span-safe specificity, tail rate, and G4 all stable. Actions: (a) register a short written synthesis of this as a named development claim with its own figure (p4f20 is the seed); (b) add it to `PAPER_CONCLUSION_SKELETON.md` as sentence 6: *"Structural convergence of the averaged transport operator does not guarantee stability of sparse selections or of all causal endpoints; instrument-invariance must be tested at the level of the scientific conclusion"* — licensed at development tier now, upgraded by the A1000 gate; (c) note its outward implication for the paper's related-work section: any published J-lens result whose selections came from a single small fit inherits this sensitivity unless shown otherwise.

## 3. Amendments

**3.1 Draft the Q-L2 amendment before opening A1000.** 4_3 correctly says the selected-span-not-token-rows estimand redefinition must be prospective. Make that provable: write `preregistration/QL2_ESTIMAND_AMENDMENT_DRAFT.md` (span/projector as the scientific object; literal ID Jaccard demoted to methods diagnostic; frozen tolerances) and commit it *while the fit is still running*, before any A1000 comparison exists. If Q-L2 obtains, the amendment activates with a clean pre-outcome timestamp; if Q-L1 obtains, it is discarded unused. Zero cost, large credibility.

**3.2 Prompt-323 influence audit mirrors prompt-112.** Same leave-one-out identity, same equal-weight contract assertion (tiny-model reconstruction **and** the free adjacent-atomic-checkpoint check), same three-way decision wording. Register the two audits as a pair; the paper's robustness appendix wants them side by side.

**3.3 Window math and the hand-off.** At 178.5 s/prompt, n=551→1000 needs ≈22.3 h against ≈16 h of machine: plan for the fit to end this VM at ≈n=835 and complete on the next. The cross-VM recovery contract already supports this; the failure mode to forbid is "hurrying" — changing chunk size, precision, or skip rules to beat the clock. The fit finishes when it finishes.

**3.4 The orthogonalized bridge estimand deserves its one development shot.** Beyond the power argument, the answer-direction-orthogonal component is the *scientifically correct* object left open by Phase 3 (bridge-minus-answer-direction). Run the 4_3 §7.3 development rescue once, against its strict variance gate; if it fails, P4-E1 estimation-only, with the two-relation Bank-B families explicitly preserved for the Phase 5A circuit branch rather than spent.

**3.5 Cross-track service contract.** The two OLMo Bank-W capability gates (mainline freeze blockers 4) execute on the **OLMo VM** as its first responsibility (its plan §3 already accepts this) and arrive via import bundle; this VM never loads OLMo weights while the Qwen cache is load-bearing. The mainline importer applies the full §2.5 verification list, and the joint common-support calculation runs mainline, after import, from the three registered capability artifacts.

**3.6 CPU work while the fit runs.** In order: Q-L2 draft (3.1); P4-P2 pilot producer review + SESOI memo; Bank-B decision memo for PI (estimation-only vs. orthogonalized-rescue vs. bigger-bank, with the §7 arithmetic); DriveFS durability check plan; registry whole-output verification script; freeze-candidate v0.10 assembly; import-bundle validator with tests.

## 4. Freeze path and Phase 5 hook

Freeze order once A1000's branch resolves: canonical-lens decision → P4-P2 pilot (if lawful) → P4-P1 disposition → OLMo capability imports + joint support → registry/output verification → independent protocol review → PI sign-off → freeze commit + tag. The mainline's Phase 5 contribution is fixed now: the frozen instrument stack, the conclusion-skeleton sentences 1/3/6 with their final tiers, and the two side-track import bundles integrated with ancestry preserved. Do not let either side track's findings leak into mainline narrative documents before their bundles land.

## 5. Risks

(i) The reclaim that almost happened at 22:50 UTC will eventually happen — atomic checkpoints are the whole defense; verify the monitor loop is actually running. (ii) A1000 resolving Q-L4 (causal endpoints fit-sensitive) is the branch that hurts; if it lands, the methods headline is real and the published-lens binding for comparability is the honest fallback — pre-write that paragraph too. (iii) Three VMs sharing one Drive account: watch quota; side tracks must create their own roots before first write, never fall back to the Phase 4 root "temporarily."
