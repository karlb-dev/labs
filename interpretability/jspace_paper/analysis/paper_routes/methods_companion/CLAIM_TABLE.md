# Paper B claim table (P8)

| # | Sentence (maximum licensed form) | Ledger | Evidence ids |
|---|---|---|---|
| B1 | Across nested same-corpus Qwen fits, the averaged operator converges monotonically and passes every structural gate. | C6 | `p4-qwen-lens-convergence-drawA-*` |
| B2 | Occupancy, centered excess, span-safe specificity, tail rate, G4, and bridge preference are fit-stable at every boundary. | C6 | `p4-qwen-multilens-functional-gate-*` |
| B3 | Selected-ID identity never passes (median exactly 7/13 at all boundaries) and projector overlap stays below floor. | C6 | same |
| B4 | The bridge-rescue endpoint oscillates in sign along the ladder and fails its gate; Q-L4 is forced by this row. | C6 | `p4-qwen-canonical-lens-decision-a1000-dev-v1` |
| B5 | Near-ties are pervasive but do not explain the failures; the stable-core stratum fails too; zero rank-deficient sites. | C6 | `p4-qwen-selection-margin-a500-a1000-dev-v1` |
| B6 | Single-prompt influence is immaterial at the current runtime (closest metric 3,856.8× under threshold); historical-runtime reproduction is not claimed. | C6/C7 | `p4-qwen-lens-influence-prompt323-dev-v1` |
| B7 | Version-level pinning does not pin backward semantics; internally-repeatable runtimes differed materially across eras; a prospective tolerance caught it pre-outcome. | C7 | `p4-runtime-identity-synthesis-v1` |
| B8 | A target-blind mixed-precision calibration can license historical exact-backend comparisons without recompute; Study 1's failed 1e-5 gate remains immutable. | C5 | `gm2-backend-parity-calibration-v1`; `gm2-stage1-relicense-v1` |
| B9 | Gemma's five-layer finite-scale tangent mismatch is 17–68× above the frozen calibrated backend ceiling (and 12–48× the OLMo control's late anchor), directional, depth-dependent, and closed at tested scope with no mechanism named. | C5 | `gm-jvp-gemma-stage1-v1` |
| B10 | On the frozen OLMo ladder exactly one layer-dose cell is licensed (Think L56@ε=0.10); no in-band cell on either checkpoint; eligibility is a data state. | C5 | `ol2-transport-validation-joint-v1` |
| B11 | These gates bound lens-as-predictor designs, not the paired removal contrasts, whose license is their control behavior. | C5 | A5 reconciliation |
| B12 | The registered dose-to-ladder mapping is archive-unavailable; neither in-band nor out-of-band placement of causal doses may be asserted. | C5 | `ol2-transport-validation-joint-v1` |
| B13 | A preflight running backend → delivery → tangent → fit → selection → endpoint gates would have caught every failure this campaign hit; we specify it as a reporting standard. | C5–C7 | synthesis |

Forbidden anywhere in Paper B: convergence-rate language; "A1000
converged" unqualified; canonical sparse lens; "Gemma is nonlinear /
lacks a workspace"; "transport fails" unqualified; dose-coverage
claims; historical-runtime identity; re-arguing any empirical causal
claim (cite Paper A).
