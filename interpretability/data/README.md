# Frozen datasets

These CSVs are **vendored and frozen** (course rule: no live downloads at lab
runtime, no student-authored truth sets). They were generated deterministically
by the `make_*.py` generators in this directory at authoring time; regenerating
produces identical files. If a statement turns out to be wrong about the world, fix it here, bump
the note below, and treat every prior run as a different dataset version.

**Robustness note:** All headline numbers across labs sit on modest N (dozens to low hundreds of items/prompts/pairs/facts across categories after expansions). Generators and per-lab caps were increased for category diversity and statistical power, but the emphasis remains on qualitative patterns, structure, and controls rather than precise percentages. Every percentage or rate deserves at most one significant figure of confidence. See individual lab writeups for the exact N and the required control artifacts.

| File | Family | Contents |
|---|---|---|
| `truth_cities.csv` | cities | "The city of X is in Y." — 56 true / 56 false (expanded) |
| `truth_comparisons.csv` | comparisons | "Sixty-one is larger than fourteen." — 72 true / 72 false (expanded) |
| `truth_negations.csv` | negations | "The city of X is not in Y." — 56 true / 56 false (expanded); labels are truth values, so surface form anti-correlates with the affirmative family |
| `truth_misconceptions.csv` | misconceptions | 36 popular **false** beliefs + their corrections (36 true / 36 false). The only family where assertion frequency in text anti-correlates with truth — the truth-vs-plausibility stress test. Generated by `make_belief_sets.py`. |

Columns: `statement_id, family, statement, label (1=true), meta`.

Used by: Lab 4 (probing). The mass-mean truth direction Lab 4 saves from these
statements is reused causally by Lab 7. The misconceptions family is excluded
from Lab 4's layer/direction selection (it is the held-out stress test, not a
driver of the saved artifact).

### Property-probe sets (controls + advanced-lab seeds)

| File | Label semantics | Contents |
|---|---|---|
| `affect_valence.csv` | 1 = positive valence | 24 topic-matched positive/negative statement pairs |
| `epistemic_certainty.csv` | 1 = confident assertion | 20 content-matched confident/hedged pairs (identical proposition, different epistemic marking) |

Same schema as the truth files, but the labels are NOT truth values: they
let you train a valence or certainty probe with the exact Lab 4 machinery
and ask whether your "truth direction" is secretly tracking confidence or
sentiment. Generated by `make_belief_sets.py`. Not wired into any default
lab run; see `advanced_labs_data_roadmap.md`.

## Lab 1 / Lab 2 multi-relation probe sets

| File | Contents |
|---|---|
| `relation_probes_lab1.csv` | 105 prompts across 12 relation classes (capitals, languages, antonyms, colors, taxonomy, counting, body, tools, sequences, materials, habitats, emotion-outcomes), Lab 1 schema |
| `relation_pairs_lab2.csv` | the same items as 210 Lab 2 rows: each prompt twice, once with a same-class ("hard", instance discrimination) and once with a cross-class ("easy", class discrimination) distractor |

Every target and distractor is verified single-token under BOTH course
tokenizers (gpt2 + Olmo-3) at authoring time. The `emotion_outcome` class
contains deliberate near-ties (tagged `near_tie=1` in the note column).
Generated by `make_relation_sets.py`. Run with:

```bash
python interp_bench.py --lab lab1 --prompt-set data/relation_probes_lab1.csv
python interp_bench.py --lab lab2 --prompt-set data/relation_pairs_lab2.csv
```

## Lab 7 steering datasets

| File | Contents |
|---|---|
| `refusal_elicitation_set.csv` | 28 pairs: a refusal-eliciting (harmful-*sounding*) instruction and a length/structure-matched benign one (expanded) |
| `sentiment_contrast_set.csv` | 28 positive/negative sentiment statement pairs (matched topic, expanded) |
| `steering_eval_prompts.csv` | 24 neutral benign prompts to steer and score (expanded) |

Known scope limit: several eval prompts share topics with sentiment contrast
pairs (e.g. morning, meal, weather, book, workday, plus story/novel, music/
album, and museum/concert overlaps in the expanded sets). Per-pair
differencing cancels topic within the direction, but the dose-response claim
is therefore about these topics, not sentiment-in-general; the drift task is
the only off-topic check.

**Safety constraints on `refusal_elicitation_set.csv` (do not weaken):**

- The refusal-eliciting column is **category-level with no operational
  content** — it exists only to elicit the model's internal *refusal
  representation* via forward passes. It contains nothing actionable.
- Lab 7 **never samples a completion from any refusal-set prompt.** It
  computes a difference-in-means direction over activations, uses it as a
  forward-pass monitor, and steers only **benign** prompts *toward* refusal.
- The lab **does not implement refusal ablation** (the jailbreak result is
  assigned as reading, not reproduced).
- Students never author or extend this file. The pairing (refusal vs matched
  benign) is what makes the diff-in-means isolate refusal rather than surface
  form; breaking the pairing breaks the method.

Generated by `make_steering_sets.py`. Version note: v2, 2026-06-11 (expanded refusal to 28, sentiment 28, eval prompts 24).

## Lab 8 feature-interpretation corpus

| File | Contents |
|---|---|
| `sae_feature_corpus.csv` | 259 short entries: 23–29 across 10 single domains (253) + 6 deliberately mixed lines (expanded) |

Columns: `text_id, domain, text`. Domains are `chemistry, code, cooking,
emotion, finance, history, law, medicine, sports, weather`; mixed lines use a
`a+b` compound tag and are excluded from single-domain ground truth.

This corpus is the substrate the SAE is run over to find, label, and
**validate** features. It is built for validation, not just diversity:

- **Domain tags are the held-out ground truth.** A label like "fires on
  chemistry" is tested by whether the feature fires on held-out chemistry
  lines and stays quiet elsewhere — precision/recall, not vibes.
- **Domains come in confusable pairs by surface token** so a semantic feature
  can be told apart from a lexical one: chemistry/cooking (acid, base, salt,
  reduce), finance/sports (score, lead, beat, rally), law/medicine (trial,
  discharge, administer), weather/emotion (storm, cloud, bright, cold). The
  adversarial near-miss prompts in Lab 8 are drawn from these pairs.
- **Mixed lines** blend two domains so a genuinely polysemantic feature has
  something honest to fire on.

Generated by `make_sae_corpus.py`. Version note: v2, 2026-06-11 (expanded lines per domain, cleaned finance list; finance keeps a "lead" line so every documented finance/sports confusable token is backed on both sides).

## Lab 10 / Lab 11 MCQ items

| File | Contents |
|---|---|
| `mcq_items.csv` | 140 four-option MCQ items across 7 MMLU subjects, short questions only |

Columns: `id, domain, question, option_a..option_d, answer_key (A–D)`.

Sampled deterministically (seed 0) from the MMLU test split at authoring time
by `make_mcq_items.py` — the **only** generator here that needs network, and
only when regenerating. The answer keys are MMLU's; the small published rate
of MMLU key errors is one more reason every Lab 10/11 claim is scoped "on
this dataset". Lab 10 samples items by stride; Lab 11's `cot_faithfulness`
audit domain deliberately uses an **offset** stride so the capstone audits a
fresh slice rather than the items that formed the student's beliefs.

Generated by `make_mcq_items.py`. Version note: v2, 2026-06-12 — ids carry the full subject name (v1 truncated to 12 chars, which collapsed the two `high_school_*` subjects into duplicate ids; same questions, new ids, so per-item joins across runs made before/after v2 must map by question text).

## Lab 12 relation-geometry set

| File | Contents |
|---|---|
| `advanced_relation_geometry.csv` | 244 single-token relation items across 12 families, organized around three relation-swap groups |
| `MANIFEST.json` | sha256, row counts, and verified tokenizers for advanced-course frozen CSVs (starts the data-pinning convention from the advanced outline) |

Columns: `item_id, family, swap_group, entity_group, template, trailing,
prompt, relword, subject, target, hard_distractor, easy_distractor, note`.

- **Three relation-swap groups** hold subjects and template skeleton fixed
  while only the relation word changes: `country_sem` (capital_of /
  language_of / continent_of over 27 shared countries), `adj_morph`
  (opposite_of / comparative_of over 24 shared adjectives), `month_seq`
  (month_after / month_before over the 12 months). Inside a group, prompts
  for one subject are token-aligned and differ at exactly the relation-word
  position — the entity-class and template-syntax confounds die by
  construction, and relation-swap patch pairs exist by construction.
- **Five breadth families** (currency_of, plural_of, color_of, material_of,
  home_of) add entity-class variety for the cosine atlas; two of them end in
  "is the" (`trailing` column) because bare "is" invites an article.
- **Two distractors per item**: hard (same answer class, cyclic next distinct
  target in the family) and easy (fixed cross-family donor word) — the
  dual-distractor margin measurement from the Lab 1/2 relation sets, kept.
- **Dual-tokenizer verified**: every subject, relation word, target, and
  distractor is a single token with leading space under BOTH gpt2 and the
  Olmo-3 tokenizer, checked at generation time; the lab re-verifies at
  runtime against whatever tokenizer it actually loaded and drops with an
  audit row. Token positions are never stored — they are located at runtime.
- Authoring-time casualties, on purpose: `superlative` is 3 BPE tokens (so
  the morphology group is a pair), and an authorship family died because
  book titles are almost never single tokens. The generator prints every
  dropped candidate; absences are frame limitations, not evidence.

Generated by `make_advanced_relation_sets.py` (loads both tokenizers; the
only randomness-free filter is tokenization itself). Version note: v1,
2026-06-12.

## Lab 13 affect/emotion geometry set

| File | Contents |
|---|---|
| `affect_emotion_pairs.csv` | 24 target emotion pairs plus 6 confound pairs for read/write affect geometry |
| `MANIFEST.json` | sha256, row counts, emotion counts, and confound counts for advanced-course frozen CSVs |

Columns: `item_id, emotion, cause, arousal, valence, content_text,
neutral_text, generation_prompt, neutral_generation_prompt, confound, note`.

- **Target emotions**: `joy`, `sadness`, `anger`, and `fear`, with six causes
  per emotion. Every target row has an emotion-laden sentence and a neutral
  paraphrase about the same cause/topic, plus matched generation prompts.
- **Read/write bridge**: `content_text` vs `neutral_text` feeds the
  comprehension direction; `generation_prompt` vs
  `neutral_generation_prompt` feeds the generation direction. Lab 13 tests
  whether those directions transfer across modes.
- **Confounds**: `surprising_neutral`, `positive_calm`, and
  `arousal_neutral` rows are not target labels. They are audit probes for
  cheap explanations: surprise words, pleasant low-arousal positivity, and
  generic activation/arousal.
- **No tokenization filter**: unlike Lab 12, this lab operates on sentence
  prompts and chat templates, not single-token answers. The manifest records
  the data hash rather than verified token IDs.

Generated by `make_emotion_pairs.py`. Version note: v1, 2026-06-12.

## Lab 14 certainty/calibration set

| File | Contents |
|---|---|
| `certainty_calibration_items.csv` | 36 fixed-choice calibration items across MCQ, factual QA, and free-form answerability |
| `MANIFEST.json` | sha256, family counts, and answerable/unanswerable counts for advanced-course frozen CSVs |

Columns: `item_id, family, topic, answerable, question, option_a,
option_b, option_c, option_d, answer_key, expected_answer, answer_format,
confident_statement, hedged_statement, note`.

- **Families**: `mcq`, `factual_qa`, and `freeform_answerability`, each
  balanced between known-answerable and known-unanswerable items.
- **Fixed choice frame**: every item is scored through A/B/C/D logits, so Lab
  14 can compute entropy, margins, and correctness without free-form grading.
  Known-unanswerable items use `D = Cannot be determined from the information
  given`.
- **Style control**: every row has a confident statement and a hedged
  statement with matched proposition content. Lab 14 uses these to build a
  hedging-style direction separate from answerability.
- **MCQ source**: answerable MCQ rows are drawn deterministically from
  `mcq_items.csv`; unanswerable MCQ-shaped rows are authored controls.

Generated by `make_certainty_calibration.py`. Version note: v1, 2026-06-12.

## Lab 16 sycophancy/user-belief set

| File | Contents |
|---|---|
| `sycophancy_pressure_items.csv` | 240 pressure-prompt rows from 40 base misconceptions across science, math, history, trivia, and technology |
| `MANIFEST.json` | sha256, domain counts, condition counts, and base-fact count for advanced-course frozen CSVs |

Columns: `row_id, base_id, domain, topic, condition, pressure_level,
user_belief_label, question, correct_answer, misconception_answer,
user_belief, user_message, correct_keywords, sycophantic_keywords,
mixed_keywords, rubric, note`.

- **Base facts**: eight base facts per domain, each with a correct answer and
  a common misconception answer. The facts are intentionally ordinary; the
  lab is about the pressure channel, not fact discovery.
- **Six variants per fact**: neutral question, correct-belief control,
  false-belief prompt, mild pressure, authority pressure, and identity
  pressure. All six variants stay together in train/eval splits.
- **Scoring scaffold**: keyword rubrics label `correct`, `sycophantic`,
  `mixed`, or `ambiguous` generations, and Lab 16 exports blank hand-label
  columns for audit samples before any strong behavioral claim.
- **Controls by construction**: the correct-belief and false-belief variants
  share the same question and differ in the asserted user answer; politeness
  and agreement controls are built in the lab runner rather than the CSV.

Generated by `make_sycophancy_pressure.py`. Version note: v1, 2026-06-13.

## Lab 17 persona/register/voice set

| File | Contents |
|---|---|
| `persona_register_pairs.csv` | 24 paired contrast rows: persona, register, voice, and agreement frames over matched task content |
| `MANIFEST.json` | sha256, family counts, trait counts, and task-kind counts for advanced-course frozen CSVs |

Columns: `item_id, trait, family, topic, task_kind, positive_label,
negative_label, prompt_positive, prompt_negative, eval_prompt,
expected_keywords, positive_markers, negative_markers, content_question,
note`.

- **Traits**: patient museum guide, technical expert register, warm
  supportive voice, and honest correction. Each has a matched negative/control
  frame over the same task.
- **Task content**: six compact tasks cover coding, math, factual QA, science,
  planning, and writing. Lab 17 splits by trait/topic so directions are trained
  on one subset and evaluated on held-out topics.
- **Content-vs-style audit**: every row carries expected content keywords plus
  positive and negative style markers. Lab 17 scores them separately so a
  "persona" or "register" effect cannot hide content degradation.
- **Authorship scope**: this file is not an authorship corpus. Paper-corpus
  attribution is a later optional mode only after topic/model/process
  provenance is balanced.

Generated by `make_persona_register_pairs.py`. Version note: v1, 2026-06-13.

## Lab 18 humor/incongruity set

| File | Contents |
|---|---|
| `humor_incongruity_pairs.csv` | 20 authored micro-scenes across pun/wordplay, expectation violation, caption scene, and resolution-twist families |
| `MANIFEST.json` | sha256, family counts, condition counts, and source note for advanced-course frozen CSVs |

Columns: `item_id, family, setup, joke_completion, literal_completion,
surprise_completion, silly_completion, positive_completion, setup_anchor,
resolution_keyword, joke_markers, silly_markers, surprise_markers,
positive_markers, note`.

- **Matched endings**: every setup has one joke ending plus literal,
  surprising-not-funny, silly-not-joke, and positive-not-joke controls.
- **Deflationary controls by construction**: surprise, silliness, and
  positivity are not afterthoughts; they are condition columns used directly
  by the Lab 18 direction, cosine, and steering audits.
- **Authored micro-scenes**: the rows are short course-authored snippets, not
  quoted joke corpora. This keeps the lab free of long copyrighted excerpts
  and keeps each row inspectable.
- **Marker scaffold**: each row carries marker specs for quick generation
  scoring, but any writeup must fill the hand-label columns in the exported
  steering table before claiming that outputs got funnier.

Generated by `make_humor_incongruity.py`. Version note: v1, 2026-06-13.

## Lab 26 causal-abstraction set

| File | Contents |
|---|---|
| `causal_abstraction_tasks.csv` | 30 residual-resampling tasks across induction copying and relation-answer domains |
| `MANIFEST.json` | sha256, domain counts, task counts, and row count for the frozen Lab 26 CSV |

Columns: `item_id, domain, split, high_level_task, template_family, prompt,
target, distractor, source_token, source_position, target_position,
relation_family, subject, answer, high_level_variables_json`.

- **Induction rows**: repeated triplets such as `red blue green red blue green
  red blue`, where `COPY_SOURCE` is the token after the previous `QUERY_TOKEN`.
  Donors can preserve `COPY_SOURCE` while varying the surface frame, or break
  `COPY_SOURCE` while preserving the query token.
- **Relation rows**: matched country prompts for `capital_of`, `language_of`,
  and `continent_of`. Donors can preserve `RELATION` while varying `SUBJECT`,
  or preserve `SUBJECT` while breaking `RELATION`.
- **Position semantics**: `source_position` is the hypothesis-relevant token
  position (`COPY_SOURCE` for induction, relation word for relation prompts);
  `target_position` is the final prompt token. Lab 26 re-verifies these
  positions at runtime before patching.
- **Formal-spec pair**: `specs/lab26_induction_hypothesis.json` and
  `specs/lab26_relation_hypothesis.json` define the preregistered abstraction
  maps and thresholds used by the default run.

Generated by hand-authored deterministic course rows, aligned with the Lab 26
formal specs. Version note: v1, 2026-06-14.

## Lab 27 path-mediation set

| File | Contents |
|---|---|
| `path_mediation_tasks.csv` | 9 clean/corrupt path-mediation tasks across induction, factual recall, and relation-swap domains |
| `MANIFEST.json` | sha256, domain counts, row count, and pairing notes for the frozen Lab 27 CSV |

Columns: `item_id, domain, prompt, clean_prompt, corrupt_prompt, target,
distractor, positions_json, candidate_nodes_json, candidate_paths_json`.

- **Induction rows** reuse repeated-token copy prompts, with source positions
  at the previous answer token and receiver positions at the final query token.
- **Factual rows** use subject-swapped capital prompts, matching the Lab 5
  clean/corrupt convention.
- **Relation rows** use same-subject capital/language prompts, matching the
  Lab 12 relation-swap control.
- **Path scope**: this dataset supports Lab 27's residual two-site mediation
  proxy. It is not evidence for a unique attention edge unless a later
  implementation adds receiver-specific path hooks.

Generated by hand-authored deterministic course rows. Version note: v1,
2026-06-14.

## Lab 31 auto-interpretability tasks

| File | Contents |
|---|---|
| `auto_interp_feature_tasks.jsonl` | 10 synthetic/gold feature-label tasks with top contexts, held-out positives, hard negatives, confusables, and decoys |
| `MANIFEST.json` | sha256, feature-type counts, label counts, and row count for the frozen Lab 31 JSONL |

Each JSONL row includes `feature_id`, `model`, `layer`, `feature_index`,
`feature_type`, `top_contexts`, `heldout_contexts`, `negative_contexts`,
`confusable_contexts`, `adversarial_contexts`, `gold_label`,
`gold_label_secondary`, and `expected_abstain`.

- **Synthetic gold labels**: eight rows have known domain labels so label
  recovery can be scored without an LLM judge.
- **High-risk controls**: one polysemantic feature and one random-control
  feature should trigger abstention or human review rather than forced labels.
- **Confusable tests**: every feature includes hard negatives and decoys that
  share surface words with the target label.
- **Human-review fields**: Lab 31 exports shared blank review columns in its
  tables; labels should not be trusted in writeups until those are filled.

Generated by hand-authored deterministic course rows. Version note: v1,
2026-06-14.

## Lab 32 preference-circuit pairs

| File | Contents |
|---|---|
| `preference_circuit_pairs.csv` | 20 benign paired-response preference rows with A/B balance and shortcut confounds |
| `MANIFEST.json` | sha256, domain counts, preference-type counts, confound counts, row count, and safety scope for the frozen Lab 32 CSV |

Columns: `pair_id, domain, prompt, response_a, response_b, preferred,
preference_type, confound_type, split, notes`.

- **Canonical Tier A path**: Lab 32 computes a DPO-style policy/reference
  log-prob-ratio proxy with a response-unconditional unigram reference.
- **Shortcut controls**: rows deliberately include length, politeness,
  agreement, sentiment, hedging, refusal, confidence, and specificity
  confounds.
- **Sycophancy risk**: false-belief and false-agreement rows test whether
  agreement with the user is mistaken for preference.
- **Safety scope**: the rows are benign and do not contain harmful prompt
  optimization, jailbreak search, real private data, or refusal-ablation tasks.
- **Human-review fields**: Lab 32 exports shared blank review columns; labels
  should not be cited in writeups until reviewed.

Generated by hand-authored deterministic course rows. Version note: v1,
2026-06-14.

## Lab 28 editing/unlearning set

| File | Contents |
|---|---|
| `editing_unlearning_targets.csv` | 5 benign counterfactual edit targets with donor prompts, paraphrases, retain prompts, and neighbor prompts |
| `MANIFEST.json` | sha256, family counts, edit-type counts, row count, and safety scope for the frozen Lab 28 CSV |

Columns: `target_id, family, edit_type, prompt, target_before,
target_after, retain_prompts_json, paraphrase_prompts_json,
neighbor_prompts_json, safety_notes, donor_prompt`.

- **Safety scope**: every row is a harmless public association or calendar
  sequence. The lab evaluates next-token logits only; it does not persist
  weight edits, use private data, or touch refusal/jailbreak behavior.
- **Donor prompts**: each target has a benign donor prompt whose final-token
  residual stream supplies the counterfactual activation direction.
- **Audit prompts**: paraphrases test prompt robustness; retain and neighbor
  prompts test whether the edit damages nearby or unrelated associations.
- **Tokenization rule**: target, counterfactual, retain, and distractor answer
  strings are runtime-verified as single tokens by Lab 28 before any edit.

Generated by hand-authored deterministic course rows. Version note: v1,
2026-06-14.

## Lab 29 training-dynamics set

| File | Contents |
|---|---|
| `training_dynamics_tasks.csv` | 11 frozen evaluation rows for controlled tiny-transformer checkpoint dynamics |
| `MANIFEST.json` | sha256, family counts, split counts, and row count for the frozen Lab 29 CSV |

Columns: `item_id, task_family, prompt, target, distractor, split,
expected_mechanism, notes`.

- **Induction-copy rows**: fixed prompts of the form `a b c a b -> c` across
  train, held-out, and test splits. The tiny model trains on generated
  induction-copy batches and evaluates on these frozen rows.
- **Untrained controls**: relation and calendar rows are present in the tiny
  vocabulary but never trained. They detect broad output drift and
  overinterpretation of improving margins.
- **Controlled scope**: Lab 29's default run is a deterministic construction
  for learning checkpoint methodology. External checkpoint sequences should
  reuse the same artifact schema and claim grammar rather than overwriting
  these rows.

Generated by hand-authored deterministic course rows. Version note: v1,
2026-06-14.

## Lab 30 feature-lineage corpus

| File | Contents |
|---|---|
| `feature_lineage_corpus.csv` | 32 domain-labeled texts across four confusable domain pairs for prototype-direction lineage |
| `MANIFEST.json` | sha256, domain counts, split counts, and row count for the frozen Lab 30 CSV |

Columns: `row_id, family, domain, source_lab, text, group_id, split,
labels_json`.

- **Confusable pairs**: code/cooking, finance/sports, law/medicine, and
  weather/emotion deliberately share surface vocabulary pressure so lineage
  claims must beat plausible label-confusion controls.
- **Prototype scope**: the default lab builds supervised mean-minus-rest
  directions across residual depths. It does not claim SAE or crosscoder
  feature identity.
- **Marker tokens**: `labels_json` supplies single-token marker/contrast pairs
  for the narrow activation-addition transfer probe. Lab 30 re-verifies those
  tokens at runtime.

Generated by hand-authored deterministic course rows. Version note: v1,
2026-06-14.
